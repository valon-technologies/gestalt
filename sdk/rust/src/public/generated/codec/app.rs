// Code generated by sdkgen. DO NOT EDIT.

//! Generated wire conversions for app.proto.

#![allow(clippy::all, unused_variables, unused_mut, dead_code)]

use crate::generated::v1;
use crate::public::generated::app::{
    AgentToolRef, AppInvokeGraphQLRequest, AppInvokeRequest, OperationAnnotations, OperationResult,
    StringList, SubjectContext, SubjectPermissionContext,
};
use crate::public::generated::codec::support::to_wire_struct;

/// Converts a native `AgentToolRef` to its wire message.
pub(crate) fn to_wire_agent_tool_ref(value: AgentToolRef) -> v1::AgentToolRef {
    v1::AgentToolRef {
        app: value.app,
        operation: value.operation,
        connection: value.connection,
        instance: value.instance,
        title: value.title,
        description: value.description,
        credential_mode: value.credential_mode,
        system: value.system,
        run_as: value.run_as.map(to_wire_subject_context),
    }
}

/// Converts a wire `AgentToolRef` to its native message.
pub(crate) fn from_wire_agent_tool_ref(value: v1::AgentToolRef) -> AgentToolRef {
    AgentToolRef {
        app: value.app,
        operation: value.operation,
        connection: value.connection,
        instance: value.instance,
        title: value.title,
        description: value.description,
        credential_mode: value.credential_mode,
        system: value.system,
        run_as: value.run_as.map(from_wire_subject_context),
    }
}

/// Encodes a wire `SubjectPermissionContext` as protobuf JSON.
pub(crate) fn encode_wire_subject_permission_context_json(
    value: &v1::SubjectPermissionContext,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.app.is_empty() {
        object.insert("app".into(), serde_json::Value::String(value.app.clone()));
    }
    if !value.operations.is_empty() {
        object.insert(
            "operations".into(),
            serde_json::Value::Array(
                value
                    .operations
                    .iter()
                    .map(|item| serde_json::Value::String(item.to_string()))
                    .collect(),
            ),
        );
    }
    if value.all_operations {
        object.insert(
            "allOperations".into(),
            serde_json::Value::Bool(value.all_operations),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `SubjectPermissionContext`.
pub(crate) fn decode_wire_subject_permission_context_json(
    value: &serde_json::Value,
) -> Result<v1::SubjectPermissionContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::SubjectPermissionContext {
        app: object
            .get("app")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        operations: match object.get("operations") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for operations")
                })?
                .iter()
                .map(|item| {
                    Ok::<String, crate::rpc_support::GestaltError>(
                        item.as_str().unwrap_or_default().to_string(),
                    )
                })
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
        all_operations: object
            .get("allOperations")
            .and_then(|value| value.as_bool())
            .unwrap_or(false),
    })
}

/// Encodes a wire `SubjectContext` as protobuf JSON.
pub(crate) fn encode_wire_subject_context_json(value: &v1::SubjectContext) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.id.is_empty() {
        object.insert("id".into(), serde_json::Value::String(value.id.clone()));
    }
    if !value.email.is_empty() {
        object.insert(
            "email".into(),
            serde_json::Value::String(value.email.clone()),
        );
    }
    if !value.display_name.is_empty() {
        object.insert(
            "displayName".into(),
            serde_json::Value::String(value.display_name.clone()),
        );
    }
    if !value.scopes.is_empty() {
        object.insert(
            "scopes".into(),
            serde_json::Value::Array(
                value
                    .scopes
                    .iter()
                    .map(|item| serde_json::Value::String(item.to_string()))
                    .collect(),
            ),
        );
    }
    if !value.permissions.is_empty() {
        object.insert(
            "permissions".into(),
            serde_json::Value::Array(
                value
                    .permissions
                    .iter()
                    .map(|item| encode_wire_subject_permission_context_json(item))
                    .collect(),
            ),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `SubjectContext`.
pub(crate) fn decode_wire_subject_context_json(
    value: &serde_json::Value,
) -> Result<v1::SubjectContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::SubjectContext {
        id: object
            .get("id")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        email: object
            .get("email")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        display_name: object
            .get("displayName")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        scopes: match object.get("scopes") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for scopes")
                })?
                .iter()
                .map(|item| {
                    Ok::<String, crate::rpc_support::GestaltError>(
                        item.as_str().unwrap_or_default().to_string(),
                    )
                })
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
        permissions: match object.get("permissions") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for permissions")
                })?
                .iter()
                .map(|item| decode_wire_subject_permission_context_json(item))
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
    })
}

/// Encodes a wire `AgentToolRef` as protobuf JSON.
pub(crate) fn encode_wire_agent_tool_ref_json(value: &v1::AgentToolRef) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.app.is_empty() {
        object.insert("app".into(), serde_json::Value::String(value.app.clone()));
    }
    if !value.operation.is_empty() {
        object.insert(
            "operation".into(),
            serde_json::Value::String(value.operation.clone()),
        );
    }
    if !value.connection.is_empty() {
        object.insert(
            "connection".into(),
            serde_json::Value::String(value.connection.clone()),
        );
    }
    if !value.instance.is_empty() {
        object.insert(
            "instance".into(),
            serde_json::Value::String(value.instance.clone()),
        );
    }
    if !value.title.is_empty() {
        object.insert(
            "title".into(),
            serde_json::Value::String(value.title.clone()),
        );
    }
    if !value.description.is_empty() {
        object.insert(
            "description".into(),
            serde_json::Value::String(value.description.clone()),
        );
    }
    if !value.credential_mode.is_empty() {
        object.insert(
            "credentialMode".into(),
            serde_json::Value::String(value.credential_mode.clone()),
        );
    }
    if !value.system.is_empty() {
        object.insert(
            "system".into(),
            serde_json::Value::String(value.system.clone()),
        );
    }
    if let Some(inner) = &value.run_as {
        object.insert("runAs".into(), encode_wire_subject_context_json(&inner));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `AgentToolRef`.
pub(crate) fn decode_wire_agent_tool_ref_json(
    value: &serde_json::Value,
) -> Result<v1::AgentToolRef, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::AgentToolRef {
        app: object
            .get("app")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        operation: object
            .get("operation")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        connection: object
            .get("connection")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        instance: object
            .get("instance")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        title: object
            .get("title")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        description: object
            .get("description")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        credential_mode: object
            .get("credentialMode")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        system: object
            .get("system")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        run_as: object
            .get("runAs")
            .map(|value| decode_wire_subject_context_json(value))
            .transpose()?,
    })
}

/// Converts a native `AppInvokeGraphQLRequest` to its wire message.
pub(crate) fn to_wire_app_invoke_graphql_request(
    value: AppInvokeGraphQLRequest,
) -> v1::AppInvokeGraphQlRequest {
    v1::AppInvokeGraphQlRequest {
        app: value.app,
        document: value.document,
        variables: value.variables.map(to_wire_struct),
        connection: value.connection,
        instance: value.instance,
        idempotency_key: value.idempotency_key,
        context: None,
    }
}

/// Encodes a wire `CredentialContext` as protobuf JSON.
pub(crate) fn encode_wire_credential_context_json(
    value: &v1::CredentialContext,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.mode.is_empty() {
        object.insert("mode".into(), serde_json::Value::String(value.mode.clone()));
    }
    if !value.subject_id.is_empty() {
        object.insert(
            "subjectId".into(),
            serde_json::Value::String(value.subject_id.clone()),
        );
    }
    if !value.connection.is_empty() {
        object.insert(
            "connection".into(),
            serde_json::Value::String(value.connection.clone()),
        );
    }
    if !value.instance.is_empty() {
        object.insert(
            "instance".into(),
            serde_json::Value::String(value.instance.clone()),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `CredentialContext`.
pub(crate) fn decode_wire_credential_context_json(
    value: &serde_json::Value,
) -> Result<v1::CredentialContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::CredentialContext {
        mode: object
            .get("mode")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        subject_id: object
            .get("subjectId")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        connection: object
            .get("connection")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        instance: object
            .get("instance")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `AccessContext` as protobuf JSON.
pub(crate) fn encode_wire_access_context_json(value: &v1::AccessContext) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.policy.is_empty() {
        object.insert(
            "policy".into(),
            serde_json::Value::String(value.policy.clone()),
        );
    }
    if !value.role.is_empty() {
        object.insert("role".into(), serde_json::Value::String(value.role.clone()));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `AccessContext`.
pub(crate) fn decode_wire_access_context_json(
    value: &serde_json::Value,
) -> Result<v1::AccessContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::AccessContext {
        policy: object
            .get("policy")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        role: object
            .get("role")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `HostContext` as protobuf JSON.
pub(crate) fn encode_wire_host_context_json(value: &v1::HostContext) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.public_base_url.is_empty() {
        object.insert(
            "publicBaseUrl".into(),
            serde_json::Value::String(value.public_base_url.clone()),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `HostContext`.
pub(crate) fn decode_wire_host_context_json(
    value: &serde_json::Value,
) -> Result<v1::HostContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::HostContext {
        public_base_url: object
            .get("publicBaseUrl")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `ProviderContext` as protobuf JSON.
pub(crate) fn encode_wire_provider_context_json(value: &v1::ProviderContext) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.kind.is_empty() {
        object.insert("kind".into(), serde_json::Value::String(value.kind.clone()));
    }
    if !value.name.is_empty() {
        object.insert("name".into(), serde_json::Value::String(value.name.clone()));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `ProviderContext`.
pub(crate) fn decode_wire_provider_context_json(
    value: &serde_json::Value,
) -> Result<v1::ProviderContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::ProviderContext {
        kind: object
            .get("kind")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        name: object
            .get("name")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `InvocationContext` as protobuf JSON.
pub(crate) fn encode_wire_invocation_context_json(
    value: &v1::InvocationContext,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.request_id.is_empty() {
        object.insert(
            "requestId".into(),
            serde_json::Value::String(value.request_id.clone()),
        );
    }
    if value.depth != 0 {
        object.insert("depth".into(), serde_json::json!(value.depth));
    }
    if !value.call_chain.is_empty() {
        object.insert(
            "callChain".into(),
            serde_json::Value::Array(
                value
                    .call_chain
                    .iter()
                    .map(|item| serde_json::Value::String(item.to_string()))
                    .collect(),
            ),
        );
    }
    if !value.surface.is_empty() {
        object.insert(
            "surface".into(),
            serde_json::Value::String(value.surface.clone()),
        );
    }
    if value.internal_connection_access {
        object.insert(
            "internalConnectionAccess".into(),
            serde_json::Value::Bool(value.internal_connection_access),
        );
    }
    if !value.connection.is_empty() {
        object.insert(
            "connection".into(),
            serde_json::Value::String(value.connection.clone()),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `InvocationContext`.
pub(crate) fn decode_wire_invocation_context_json(
    value: &serde_json::Value,
) -> Result<v1::InvocationContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::InvocationContext {
        request_id: object
            .get("requestId")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        depth: object
            .get("depth")
            .and_then(|value| value.as_i64())
            .unwrap_or(0) as i32,
        call_chain: match object.get("callChain") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for callChain")
                })?
                .iter()
                .map(|item| {
                    Ok::<String, crate::rpc_support::GestaltError>(
                        item.as_str().unwrap_or_default().to_string(),
                    )
                })
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
        surface: object
            .get("surface")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        internal_connection_access: object
            .get("internalConnectionAccess")
            .and_then(|value| value.as_bool())
            .unwrap_or(false),
        connection: object
            .get("connection")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `RequestMetaContext` as protobuf JSON.
pub(crate) fn encode_wire_request_meta_context_json(
    value: &v1::RequestMetaContext,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.client_ip.is_empty() {
        object.insert(
            "clientIp".into(),
            serde_json::Value::String(value.client_ip.clone()),
        );
    }
    if !value.remote_addr.is_empty() {
        object.insert(
            "remoteAddr".into(),
            serde_json::Value::String(value.remote_addr.clone()),
        );
    }
    if !value.user_agent.is_empty() {
        object.insert(
            "userAgent".into(),
            serde_json::Value::String(value.user_agent.clone()),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `RequestMetaContext`.
pub(crate) fn decode_wire_request_meta_context_json(
    value: &serde_json::Value,
) -> Result<v1::RequestMetaContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::RequestMetaContext {
        client_ip: object
            .get("clientIp")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        remote_addr: object
            .get("remoteAddr")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        user_agent: object
            .get("userAgent")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `AgentInvocationContext` as protobuf JSON.
pub(crate) fn encode_wire_agent_invocation_context_json(
    value: &v1::AgentInvocationContext,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.provider_name.is_empty() {
        object.insert(
            "providerName".into(),
            serde_json::Value::String(value.provider_name.clone()),
        );
    }
    if !value.session_id.is_empty() {
        object.insert(
            "sessionId".into(),
            serde_json::Value::String(value.session_id.clone()),
        );
    }
    if !value.turn_id.is_empty() {
        object.insert(
            "turnId".into(),
            serde_json::Value::String(value.turn_id.clone()),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `AgentInvocationContext`.
pub(crate) fn decode_wire_agent_invocation_context_json(
    value: &serde_json::Value,
) -> Result<v1::AgentInvocationContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::AgentInvocationContext {
        provider_name: object
            .get("providerName")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        session_id: object
            .get("sessionId")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        turn_id: object
            .get("turnId")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
    })
}

/// Encodes a wire `RequestContext` as protobuf JSON.
pub(crate) fn encode_wire_request_context_json(value: &v1::RequestContext) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if let Some(inner) = &value.subject {
        object.insert("subject".into(), encode_wire_subject_context_json(&inner));
    }
    if let Some(inner) = &value.credential {
        object.insert(
            "credential".into(),
            encode_wire_credential_context_json(&inner),
        );
    }
    if let Some(inner) = &value.access {
        object.insert("access".into(), encode_wire_access_context_json(&inner));
    }
    if let Some(inner) = &value.workflow {
        if !inner.fields.is_empty() {
            object.insert(
                "workflow".into(),
                crate::public::proto_json::encode_struct(inner),
            );
        }
    }
    if let Some(inner) = &value.host {
        object.insert("host".into(), encode_wire_host_context_json(&inner));
    }
    if let Some(inner) = &value.agent_subject {
        object.insert(
            "agentSubject".into(),
            encode_wire_subject_context_json(&inner),
        );
    }
    if let Some(inner) = &value.caller {
        object.insert("caller".into(), encode_wire_provider_context_json(&inner));
    }
    if let Some(inner) = &value.invocation {
        object.insert(
            "invocation".into(),
            encode_wire_invocation_context_json(&inner),
        );
    }
    if !value.tool_refs.is_empty() {
        object.insert(
            "toolRefs".into(),
            serde_json::Value::Array(
                value
                    .tool_refs
                    .iter()
                    .map(|item| encode_wire_agent_tool_ref_json(item))
                    .collect(),
            ),
        );
    }
    if value.tool_refs_set {
        object.insert(
            "toolRefsSet".into(),
            serde_json::Value::Bool(value.tool_refs_set),
        );
    }
    if let Some(inner) = &value.request_meta {
        object.insert(
            "requestMeta".into(),
            encode_wire_request_meta_context_json(&inner),
        );
    }
    if let Some(inner) = &value.agent {
        object.insert(
            "agent".into(),
            encode_wire_agent_invocation_context_json(&inner),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `RequestContext`.
pub(crate) fn decode_wire_request_context_json(
    value: &serde_json::Value,
) -> Result<v1::RequestContext, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::RequestContext {
        subject: object
            .get("subject")
            .map(|value| decode_wire_subject_context_json(value))
            .transpose()?,
        credential: object
            .get("credential")
            .map(|value| decode_wire_credential_context_json(value))
            .transpose()?,
        access: object
            .get("access")
            .map(|value| decode_wire_access_context_json(value))
            .transpose()?,
        workflow: object
            .get("workflow")
            .map(|value| crate::public::proto_json::decode_struct(value))
            .transpose()?,
        host: object
            .get("host")
            .map(|value| decode_wire_host_context_json(value))
            .transpose()?,
        agent_subject: object
            .get("agentSubject")
            .map(|value| decode_wire_subject_context_json(value))
            .transpose()?,
        caller: object
            .get("caller")
            .map(|value| decode_wire_provider_context_json(value))
            .transpose()?,
        invocation: object
            .get("invocation")
            .map(|value| decode_wire_invocation_context_json(value))
            .transpose()?,
        tool_refs: match object.get("toolRefs") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for toolRefs")
                })?
                .iter()
                .map(|item| decode_wire_agent_tool_ref_json(item))
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
        tool_refs_set: object
            .get("toolRefsSet")
            .and_then(|value| value.as_bool())
            .unwrap_or(false),
        request_meta: object
            .get("requestMeta")
            .map(|value| decode_wire_request_meta_context_json(value))
            .transpose()?,
        agent: object
            .get("agent")
            .map(|value| decode_wire_agent_invocation_context_json(value))
            .transpose()?,
    })
}

/// Encodes a wire `AppInvokeGraphQlRequest` as protobuf JSON.
pub(crate) fn encode_wire_app_invoke_graphql_request_json(
    value: &v1::AppInvokeGraphQlRequest,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.app.is_empty() {
        object.insert("app".into(), serde_json::Value::String(value.app.clone()));
    }
    if !value.document.is_empty() {
        object.insert(
            "document".into(),
            serde_json::Value::String(value.document.clone()),
        );
    }
    if let Some(inner) = &value.variables {
        if !inner.fields.is_empty() {
            object.insert(
                "variables".into(),
                crate::public::proto_json::encode_struct(inner),
            );
        }
    }
    if !value.connection.is_empty() {
        object.insert(
            "connection".into(),
            serde_json::Value::String(value.connection.clone()),
        );
    }
    if !value.instance.is_empty() {
        object.insert(
            "instance".into(),
            serde_json::Value::String(value.instance.clone()),
        );
    }
    if !value.idempotency_key.is_empty() {
        object.insert(
            "idempotencyKey".into(),
            serde_json::Value::String(value.idempotency_key.clone()),
        );
    }
    if let Some(inner) = &value.context {
        object.insert("context".into(), encode_wire_request_context_json(&inner));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `AppInvokeGraphQlRequest`.
pub(crate) fn decode_wire_app_invoke_graphql_request_json(
    value: &serde_json::Value,
) -> Result<v1::AppInvokeGraphQlRequest, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::AppInvokeGraphQlRequest {
        app: object
            .get("app")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        document: object
            .get("document")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        variables: object
            .get("variables")
            .map(|value| crate::public::proto_json::decode_struct(value))
            .transpose()?,
        connection: object
            .get("connection")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        instance: object
            .get("instance")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        idempotency_key: object
            .get("idempotencyKey")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        context: object
            .get("context")
            .map(|value| decode_wire_request_context_json(value))
            .transpose()?,
    })
}

/// Converts a native `AppInvokeRequest` to its wire message.
pub(crate) fn to_wire_app_invoke_request(value: AppInvokeRequest) -> v1::AppInvokeRequest {
    v1::AppInvokeRequest {
        app: value.app,
        operation: value.operation,
        params: value.params.map(to_wire_struct),
        connection: value.connection,
        instance: value.instance,
        idempotency_key: value.idempotency_key,
        credential_mode: value.credential_mode,
        context: None,
        run_as: None,
    }
}

/// Encodes a wire `AppInvokeRequest` as protobuf JSON.
pub(crate) fn encode_wire_app_invoke_request_json(
    value: &v1::AppInvokeRequest,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.app.is_empty() {
        object.insert("app".into(), serde_json::Value::String(value.app.clone()));
    }
    if !value.operation.is_empty() {
        object.insert(
            "operation".into(),
            serde_json::Value::String(value.operation.clone()),
        );
    }
    if let Some(inner) = &value.params {
        if !inner.fields.is_empty() {
            object.insert(
                "params".into(),
                crate::public::proto_json::encode_struct(inner),
            );
        }
    }
    if !value.connection.is_empty() {
        object.insert(
            "connection".into(),
            serde_json::Value::String(value.connection.clone()),
        );
    }
    if !value.instance.is_empty() {
        object.insert(
            "instance".into(),
            serde_json::Value::String(value.instance.clone()),
        );
    }
    if !value.idempotency_key.is_empty() {
        object.insert(
            "idempotencyKey".into(),
            serde_json::Value::String(value.idempotency_key.clone()),
        );
    }
    if !value.credential_mode.is_empty() {
        object.insert(
            "credentialMode".into(),
            serde_json::Value::String(value.credential_mode.clone()),
        );
    }
    if let Some(inner) = &value.context {
        object.insert("context".into(), encode_wire_request_context_json(&inner));
    }
    if let Some(inner) = &value.run_as {
        object.insert("runAs".into(), encode_wire_subject_context_json(&inner));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `AppInvokeRequest`.
pub(crate) fn decode_wire_app_invoke_request_json(
    value: &serde_json::Value,
) -> Result<v1::AppInvokeRequest, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::AppInvokeRequest {
        app: object
            .get("app")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        operation: object
            .get("operation")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        params: object
            .get("params")
            .map(|value| crate::public::proto_json::decode_struct(value))
            .transpose()?,
        connection: object
            .get("connection")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        instance: object
            .get("instance")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        idempotency_key: object
            .get("idempotencyKey")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        credential_mode: object
            .get("credentialMode")
            .and_then(|value| value.as_str())
            .unwrap_or_default()
            .to_string(),
        context: object
            .get("context")
            .map(|value| decode_wire_request_context_json(value))
            .transpose()?,
        run_as: object
            .get("runAs")
            .map(|value| decode_wire_subject_context_json(value))
            .transpose()?,
    })
}

/// Converts a native `OperationAnnotations` to its wire message.
pub(crate) fn to_wire_operation_annotations(
    value: OperationAnnotations,
) -> v1::OperationAnnotations {
    v1::OperationAnnotations {
        read_only_hint: value.read_only_hint,
        idempotent_hint: value.idempotent_hint,
        destructive_hint: value.destructive_hint,
        open_world_hint: value.open_world_hint,
    }
}

/// Encodes a wire `OperationAnnotations` as protobuf JSON.
pub(crate) fn encode_wire_operation_annotations_json(
    value: &v1::OperationAnnotations,
) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if let Some(inner) = &value.read_only_hint {
        if *inner {
            object.insert("readOnlyHint".into(), serde_json::Value::Bool(*inner));
        }
    }
    if let Some(inner) = &value.idempotent_hint {
        if *inner {
            object.insert("idempotentHint".into(), serde_json::Value::Bool(*inner));
        }
    }
    if let Some(inner) = &value.destructive_hint {
        if *inner {
            object.insert("destructiveHint".into(), serde_json::Value::Bool(*inner));
        }
    }
    if let Some(inner) = &value.open_world_hint {
        if *inner {
            object.insert("openWorldHint".into(), serde_json::Value::Bool(*inner));
        }
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `OperationAnnotations`.
pub(crate) fn decode_wire_operation_annotations_json(
    value: &serde_json::Value,
) -> Result<v1::OperationAnnotations, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::OperationAnnotations {
        read_only_hint: object.get("readOnlyHint").and_then(|value| value.as_bool()),
        idempotent_hint: object
            .get("idempotentHint")
            .and_then(|value| value.as_bool()),
        destructive_hint: object
            .get("destructiveHint")
            .and_then(|value| value.as_bool()),
        open_world_hint: object
            .get("openWorldHint")
            .and_then(|value| value.as_bool()),
    })
}

/// Converts a wire `OperationResult` to its native message.
pub(crate) fn from_wire_operation_result(value: v1::OperationResult) -> OperationResult {
    OperationResult {
        status: value.status,
        body: value.body,
        headers: value
            .headers
            .into_iter()
            .map(|(key, item)| (key, from_wire_string_list(item)))
            .collect(),
    }
}

/// Encodes a wire `StringList` as protobuf JSON.
pub(crate) fn encode_wire_string_list_json(value: &v1::StringList) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if !value.values.is_empty() {
        object.insert(
            "values".into(),
            serde_json::Value::Array(
                value
                    .values
                    .iter()
                    .map(|item| serde_json::Value::String(item.to_string()))
                    .collect(),
            ),
        );
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `StringList`.
pub(crate) fn decode_wire_string_list_json(
    value: &serde_json::Value,
) -> Result<v1::StringList, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::StringList {
        values: match object.get("values") {
            Some(value) => value
                .as_array()
                .ok_or_else(|| {
                    crate::public::proto_json::invalid_proto_json("expected array for values")
                })?
                .iter()
                .map(|item| {
                    Ok::<String, crate::rpc_support::GestaltError>(
                        item.as_str().unwrap_or_default().to_string(),
                    )
                })
                .collect::<Result<Vec<_>, _>>()?,
            None => Vec::new(),
        },
    })
}

/// Encodes a wire `OperationResult` as protobuf JSON.
pub(crate) fn encode_wire_operation_result_json(value: &v1::OperationResult) -> serde_json::Value {
    let mut object = serde_json::Map::new();
    if value.status != 0 {
        object.insert("status".into(), serde_json::json!(value.status));
    }
    if !value.body.is_empty() {
        object.insert(
            "body".into(),
            crate::public::proto_json::encode_bytes(&value.body),
        );
    }
    if !value.headers.is_empty() {
        let mut map = serde_json::Map::new();
        for (key, value) in &value.headers {
            map.insert(key.to_string(), encode_wire_string_list_json(value));
        }
        object.insert("headers".into(), serde_json::Value::Object(map));
    }
    serde_json::Value::Object(object)
}

/// Decodes protobuf JSON into a wire `OperationResult`.
pub(crate) fn decode_wire_operation_result_json(
    value: &serde_json::Value,
) -> Result<v1::OperationResult, crate::rpc_support::GestaltError> {
    let Some(object) = value.as_object() else {
        return Err(crate::rpc_support::GestaltError::new(
            crate::rpc_support::gestalt_error_code::INVALID_ARGUMENT,
            "expected JSON object",
        ));
    };
    Ok(v1::OperationResult {
        status: object
            .get("status")
            .and_then(|value| value.as_i64())
            .unwrap_or(0) as i32,
        body: match object.get("body") {
            Some(value) => crate::public::proto_json::decode_bytes(value)?,
            None => Vec::new(),
        },
        headers: match object.get("headers") {
            Some(value) => {
                let mut out = std::collections::BTreeMap::new();
                let Some(entries) = value.as_object() else {
                    return Err(crate::public::proto_json::invalid_proto_json(
                        "expected object for map",
                    ));
                };
                for (key, value) in entries {
                    out.insert(key.to_string(), decode_wire_string_list_json(value)?);
                }
                out
            }
            None => std::collections::BTreeMap::new(),
        },
    })
}

/// Converts a wire `StringList` to its native message.
pub(crate) fn from_wire_string_list(value: v1::StringList) -> StringList {
    StringList {
        values: value.values,
    }
}

/// Converts a native `SubjectContext` to its wire message.
pub(crate) fn to_wire_subject_context(value: SubjectContext) -> v1::SubjectContext {
    v1::SubjectContext {
        id: value.id,
        email: value.email,
        display_name: value.display_name,
        scopes: value.scopes,
        permissions: value
            .permissions
            .into_iter()
            .map(to_wire_subject_permission_context)
            .collect(),
    }
}

/// Converts a wire `SubjectContext` to its native message.
pub(crate) fn from_wire_subject_context(value: v1::SubjectContext) -> SubjectContext {
    SubjectContext {
        id: value.id,
        email: value.email,
        display_name: value.display_name,
        scopes: value.scopes,
        permissions: value
            .permissions
            .into_iter()
            .map(from_wire_subject_permission_context)
            .collect(),
    }
}

/// Converts a native `SubjectPermissionContext` to its wire message.
pub(crate) fn to_wire_subject_permission_context(
    value: SubjectPermissionContext,
) -> v1::SubjectPermissionContext {
    v1::SubjectPermissionContext {
        app: value.app,
        operations: value.operations,
        all_operations: value.all_operations,
    }
}

/// Converts a wire `SubjectPermissionContext` to its native message.
pub(crate) fn from_wire_subject_permission_context(
    value: v1::SubjectPermissionContext,
) -> SubjectPermissionContext {
    SubjectPermissionContext {
        app: value.app,
        operations: value.operations,
        all_operations: value.all_operations,
    }
}

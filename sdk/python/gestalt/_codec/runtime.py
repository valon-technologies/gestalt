# Code generated by sdkgen. DO NOT EDIT.

"""Generated wire codec for runtime.proto."""

from __future__ import annotations

from typing import Any

from .. import runtime as native
from .._gen.v1 import runtime_pb2 as _runtime_pb2
from .support import from_wire_struct, to_wire_enum, to_wire_struct


def to_wire_configure_provider_request(value: native.ConfigureProviderRequest) -> Any:
    return _runtime_pb2.ConfigureProviderRequest(
        name=value.name,
        config=None if value.config is None else to_wire_struct(value.config),
        protocol_version=value.protocol_version,
    )


def from_wire_configure_provider_request(value: Any) -> native.ConfigureProviderRequest:
    return native.ConfigureProviderRequest(
        name=value.name,
        config=from_wire_struct(value.config) if value.HasField("config") else None,
        protocol_version=value.protocol_version,
    )


def to_wire_configure_provider_response(value: native.ConfigureProviderResponse) -> Any:
    return _runtime_pb2.ConfigureProviderResponse(
        protocol_version=value.protocol_version,
    )


def from_wire_configure_provider_response(
    value: Any,
) -> native.ConfigureProviderResponse:
    return native.ConfigureProviderResponse(
        protocol_version=value.protocol_version,
    )


def to_wire_health_check_response(value: native.HealthCheckResponse) -> Any:
    return _runtime_pb2.HealthCheckResponse(
        ready=value.ready,
        message=value.message,
    )


def from_wire_health_check_response(value: Any) -> native.HealthCheckResponse:
    return native.HealthCheckResponse(
        ready=value.ready,
        message=value.message,
    )


def to_wire_provider_identity(value: native.ProviderIdentity) -> Any:
    return _runtime_pb2.ProviderIdentity(
        kind=to_wire_enum(value.kind),
        name=value.name,
        display_name=value.display_name,
        description=value.description,
        version=value.version,
        warnings=value.warnings,
        min_protocol_version=value.min_protocol_version,
        max_protocol_version=value.max_protocol_version,
        capabilities=value.capabilities,
    )


def from_wire_provider_identity(value: Any) -> native.ProviderIdentity:
    return native.ProviderIdentity(
        kind=value.kind,
        name=value.name,
        display_name=value.display_name,
        description=value.description,
        version=value.version,
        warnings=list(value.warnings),
        min_protocol_version=value.min_protocol_version,
        max_protocol_version=value.max_protocol_version,
        capabilities=list(value.capabilities),
    )


def to_wire_start_runtime_provider_response(
    value: native.StartRuntimeProviderResponse,
) -> Any:
    return _runtime_pb2.StartRuntimeProviderResponse(
        protocol_version=value.protocol_version,
    )


def from_wire_start_runtime_provider_response(
    value: Any,
) -> native.StartRuntimeProviderResponse:
    return native.StartRuntimeProviderResponse(
        protocol_version=value.protocol_version,
    )

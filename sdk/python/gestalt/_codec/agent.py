# Code generated by sdkgen. DO NOT EDIT.

"""Generated wire codec for agent.proto."""

from __future__ import annotations

from typing import Any

from .. import agent as native
from .._gen.v1 import agent_pb2 as _agent_pb2
from . import app
from .support import (
    from_wire_struct,
    from_wire_timestamp,
    from_wire_value,
    to_wire_enum,
    to_wire_struct,
    to_wire_timestamp,
    to_wire_value,
)


def to_wire_agent_catalog_tool_config(value: native.AgentCatalogToolConfig) -> Any:
    return _agent_pb2.AgentCatalogToolConfig(
        refs=[app.to_wire_agent_tool_ref(item) for item in value.refs],
        tools=[to_wire_listed_agent_tool(item) for item in value.tools],
    )


def from_wire_agent_catalog_tool_config(value: Any) -> native.AgentCatalogToolConfig:
    return native.AgentCatalogToolConfig(
        refs=[app.from_wire_agent_tool_ref(item) for item in value.refs],
        tools=[from_wire_listed_agent_tool(item) for item in value.tools],
    )


def to_wire_agent_interaction(value: native.AgentInteraction) -> Any:
    return _agent_pb2.AgentInteraction(
        id=value.id,
        type=to_wire_enum(value.type),
        state=to_wire_enum(value.state),
        title=value.title,
        prompt=value.prompt,
        request=None if value.request is None else to_wire_struct(value.request),
        resolution=None
        if value.resolution is None
        else to_wire_struct(value.resolution),
        created_at=None
        if value.created_at is None
        else to_wire_timestamp(value.created_at),
        resolved_at=None
        if value.resolved_at is None
        else to_wire_timestamp(value.resolved_at),
        turn_id=value.turn_id,
        session_id=value.session_id,
    )


def from_wire_agent_interaction(value: Any) -> native.AgentInteraction:
    return native.AgentInteraction(
        id=value.id,
        type=value.type,
        state=value.state,
        title=value.title,
        prompt=value.prompt,
        request=from_wire_struct(value.request) if value.HasField("request") else None,
        resolution=from_wire_struct(value.resolution)
        if value.HasField("resolution")
        else None,
        created_at=from_wire_timestamp(value.created_at)
        if value.HasField("created_at")
        else None,
        resolved_at=from_wire_timestamp(value.resolved_at)
        if value.HasField("resolved_at")
        else None,
        turn_id=value.turn_id,
        session_id=value.session_id,
    )


def to_wire_agent_message(value: native.AgentMessage) -> Any:
    return _agent_pb2.AgentMessage(
        role=value.role,
        text=value.text,
        parts=[to_wire_agent_message_part(item) for item in value.parts],
        metadata=None if value.metadata is None else to_wire_struct(value.metadata),
    )


def from_wire_agent_message(value: Any) -> native.AgentMessage:
    return native.AgentMessage(
        role=value.role,
        text=value.text,
        parts=[from_wire_agent_message_part(item) for item in value.parts],
        metadata=from_wire_struct(value.metadata)
        if value.HasField("metadata")
        else None,
    )


def to_wire_agent_message_part(value: native.AgentMessagePart) -> Any:
    return _agent_pb2.AgentMessagePart(
        type=to_wire_enum(value.type),
        text=value.text,
        json=None if value.json is None else to_wire_struct(value.json),
        tool_call=None
        if value.tool_call is None
        else to_wire_agent_message_part_tool_call(value.tool_call),
        tool_result=None
        if value.tool_result is None
        else to_wire_agent_message_part_tool_result(value.tool_result),
        image_ref=None
        if value.image_ref is None
        else to_wire_agent_message_part_image_ref(value.image_ref),
    )


def from_wire_agent_message_part(value: Any) -> native.AgentMessagePart:
    return native.AgentMessagePart(
        type=value.type,
        text=value.text,
        json=from_wire_struct(value.json) if value.HasField("json") else None,
        tool_call=from_wire_agent_message_part_tool_call(value.tool_call)
        if value.HasField("tool_call")
        else None,
        tool_result=from_wire_agent_message_part_tool_result(value.tool_result)
        if value.HasField("tool_result")
        else None,
        image_ref=from_wire_agent_message_part_image_ref(value.image_ref)
        if value.HasField("image_ref")
        else None,
    )


def to_wire_agent_message_part_image_ref(value: native.AgentMessagePartImageRef) -> Any:
    return _agent_pb2.AgentMessagePartImageRef(
        uri=value.uri,
        mime_type=value.mime_type,
    )


def from_wire_agent_message_part_image_ref(
    value: Any,
) -> native.AgentMessagePartImageRef:
    return native.AgentMessagePartImageRef(
        uri=value.uri,
        mime_type=value.mime_type,
    )


def to_wire_agent_message_part_tool_call(value: native.AgentMessagePartToolCall) -> Any:
    return _agent_pb2.AgentMessagePartToolCall(
        id=value.id,
        tool_id=value.tool_id,
        arguments=None if value.arguments is None else to_wire_struct(value.arguments),
    )


def from_wire_agent_message_part_tool_call(
    value: Any,
) -> native.AgentMessagePartToolCall:
    return native.AgentMessagePartToolCall(
        id=value.id,
        tool_id=value.tool_id,
        arguments=from_wire_struct(value.arguments)
        if value.HasField("arguments")
        else None,
    )


def to_wire_agent_message_part_tool_result(
    value: native.AgentMessagePartToolResult,
) -> Any:
    return _agent_pb2.AgentMessagePartToolResult(
        tool_call_id=value.tool_call_id,
        status=value.status,
        content=value.content,
        output=None if value.output is None else to_wire_struct(value.output),
    )


def from_wire_agent_message_part_tool_result(
    value: Any,
) -> native.AgentMessagePartToolResult:
    return native.AgentMessagePartToolResult(
        tool_call_id=value.tool_call_id,
        status=value.status,
        content=value.content,
        output=from_wire_struct(value.output) if value.HasField("output") else None,
    )


def to_wire_agent_no_tools(_value: native.AgentNoTools) -> Any:
    return _agent_pb2.AgentNoTools()


def from_wire_agent_no_tools(_value: Any) -> native.AgentNoTools:
    return native.AgentNoTools()


def to_wire_agent_output(value: native.AgentOutput) -> Any:
    return _agent_pb2.AgentOutput(
        **to_wire_agent_output_kind(value.kind),
    )


def from_wire_agent_output(value: Any) -> native.AgentOutput:
    return native.AgentOutput(
        kind=from_wire_agent_output_kind(value),
    )


def to_wire_agent_output_kind(value: native.AgentOutputKind) -> dict[str, Any]:
    if isinstance(value, native.AgentOutputText):
        return {"text": to_wire_agent_text_output(value.value)}
    if isinstance(value, native.AgentOutputStructured):
        return {"structured": to_wire_agent_structured_output(value.value)}
    return {}


def from_wire_agent_output_kind(value: Any) -> native.AgentOutputKind:
    case = value.WhichOneof("kind")
    if case == "text":
        return native.AgentOutputText(value=from_wire_agent_text_output(value.text))
    if case == "structured":
        return native.AgentOutputStructured(
            value=from_wire_agent_structured_output(value.structured)
        )
    return None


def to_wire_agent_provider_capabilities(value: native.AgentProviderCapabilities) -> Any:
    return _agent_pb2.AgentProviderCapabilities(
        streaming_text=value.streaming_text,
        tool_calls=value.tool_calls,
        parallel_tool_calls=value.parallel_tool_calls,
        interactions=value.interactions,
        resumable_turns=value.resumable_turns,
        reasoning_summaries=value.reasoning_summaries,
        bounded_list_hydration=value.bounded_list_hydration,
        supported_tool_sources=[
            to_wire_enum(item) for item in value.supported_tool_sources
        ],
        supports_session_start=value.supports_session_start,
        supports_prepared_workspace=value.supports_prepared_workspace,
    )


def from_wire_agent_provider_capabilities(
    value: Any,
) -> native.AgentProviderCapabilities:
    return native.AgentProviderCapabilities(
        streaming_text=value.streaming_text,
        tool_calls=value.tool_calls,
        parallel_tool_calls=value.parallel_tool_calls,
        interactions=value.interactions,
        resumable_turns=value.resumable_turns,
        reasoning_summaries=value.reasoning_summaries,
        bounded_list_hydration=value.bounded_list_hydration,
        supported_tool_sources=list(value.supported_tool_sources),
        supports_session_start=value.supports_session_start,
        supports_prepared_workspace=value.supports_prepared_workspace,
    )


def to_wire_agent_session(value: native.AgentSession) -> Any:
    return _agent_pb2.AgentSession(
        id=value.id,
        provider_name=value.provider_name,
        model=value.model,
        client_ref=value.client_ref,
        state=to_wire_enum(value.state),
        metadata=None if value.metadata is None else to_wire_struct(value.metadata),
        created_by_subject_id=value.created_by_subject_id,
        created_at=None
        if value.created_at is None
        else to_wire_timestamp(value.created_at),
        updated_at=None
        if value.updated_at is None
        else to_wire_timestamp(value.updated_at),
        last_turn_at=None
        if value.last_turn_at is None
        else to_wire_timestamp(value.last_turn_at),
    )


def from_wire_agent_session(value: Any) -> native.AgentSession:
    return native.AgentSession(
        id=value.id,
        provider_name=value.provider_name,
        model=value.model,
        client_ref=value.client_ref,
        state=value.state,
        metadata=from_wire_struct(value.metadata)
        if value.HasField("metadata")
        else None,
        created_by_subject_id=value.created_by_subject_id,
        created_at=from_wire_timestamp(value.created_at)
        if value.HasField("created_at")
        else None,
        updated_at=from_wire_timestamp(value.updated_at)
        if value.HasField("updated_at")
        else None,
        last_turn_at=from_wire_timestamp(value.last_turn_at)
        if value.HasField("last_turn_at")
        else None,
    )


def to_wire_agent_session_start_config(value: native.AgentSessionStartConfig) -> Any:
    return _agent_pb2.AgentSessionStartConfig(
        hooks=[to_wire_agent_session_start_hook(item) for item in value.hooks],
    )


def from_wire_agent_session_start_config(value: Any) -> native.AgentSessionStartConfig:
    return native.AgentSessionStartConfig(
        hooks=[from_wire_agent_session_start_hook(item) for item in value.hooks],
    )


def to_wire_agent_session_start_hook(value: native.AgentSessionStartHook) -> Any:
    return _agent_pb2.AgentSessionStartHook(
        id=value.id,
        type=value.type,
        command=value.command,
        cwd=value.cwd,
        timeout=value.timeout,
        env=value.env,
        output=None
        if value.output is None
        else to_wire_agent_session_start_hook_output(value.output),
    )


def from_wire_agent_session_start_hook(value: Any) -> native.AgentSessionStartHook:
    return native.AgentSessionStartHook(
        id=value.id,
        type=value.type,
        command=list(value.command),
        cwd=value.cwd,
        timeout=value.timeout,
        env=dict(value.env),
        output=from_wire_agent_session_start_hook_output(value.output)
        if value.HasField("output")
        else None,
    )


def to_wire_agent_session_start_hook_output(
    value: native.AgentSessionStartHookOutput,
) -> Any:
    return _agent_pb2.AgentSessionStartHookOutput(
        additional_context=value.additional_context,
        metadata=value.metadata,
    )


def from_wire_agent_session_start_hook_output(
    value: Any,
) -> native.AgentSessionStartHookOutput:
    return native.AgentSessionStartHookOutput(
        additional_context=value.additional_context,
        metadata=value.metadata,
    )


def to_wire_agent_structured_output(value: native.AgentStructuredOutput) -> Any:
    return _agent_pb2.AgentStructuredOutput(
        schema=None if value.schema is None else to_wire_struct(value.schema),
    )


def from_wire_agent_structured_output(value: Any) -> native.AgentStructuredOutput:
    return native.AgentStructuredOutput(
        schema=from_wire_struct(value.schema) if value.HasField("schema") else None,
    )


def to_wire_agent_text_output(_value: native.AgentTextOutput) -> Any:
    return _agent_pb2.AgentTextOutput()


def from_wire_agent_text_output(_value: Any) -> native.AgentTextOutput:
    return native.AgentTextOutput()


def to_wire_agent_tool_config(value: native.AgentToolConfig) -> Any:
    return _agent_pb2.AgentToolConfig(
        **to_wire_agent_tool_config_source(value.source),
    )


def from_wire_agent_tool_config(value: Any) -> native.AgentToolConfig:
    return native.AgentToolConfig(
        source=from_wire_agent_tool_config_source(value),
    )


def to_wire_agent_tool_config_source(
    value: native.AgentToolConfigSource,
) -> dict[str, Any]:
    if isinstance(value, native.AgentToolConfigNone):
        return {"none": to_wire_agent_no_tools(value.value)}
    if isinstance(value, native.AgentToolConfigCatalog):
        return {"catalog": to_wire_agent_catalog_tool_config(value.value)}
    return {}


def from_wire_agent_tool_config_source(value: Any) -> native.AgentToolConfigSource:
    case = value.WhichOneof("source")
    if case == "none":
        return native.AgentToolConfigNone(value=from_wire_agent_no_tools(value.none))
    if case == "catalog":
        return native.AgentToolConfigCatalog(
            value=from_wire_agent_catalog_tool_config(value.catalog)
        )
    return None


def to_wire_agent_turn(value: native.AgentTurn) -> Any:
    return _agent_pb2.AgentTurn(
        id=value.id,
        session_id=value.session_id,
        provider_name=value.provider_name,
        model=value.model,
        status=to_wire_enum(value.status),
        messages=[to_wire_agent_message(item) for item in value.messages],
        status_message=value.status_message,
        created_by_subject_id=value.created_by_subject_id,
        created_at=None
        if value.created_at is None
        else to_wire_timestamp(value.created_at),
        started_at=None
        if value.started_at is None
        else to_wire_timestamp(value.started_at),
        completed_at=None
        if value.completed_at is None
        else to_wire_timestamp(value.completed_at),
        execution_ref=value.execution_ref,
        **to_wire_agent_turn_output(value.output),
    )


def from_wire_agent_turn(value: Any) -> native.AgentTurn:
    return native.AgentTurn(
        id=value.id,
        session_id=value.session_id,
        provider_name=value.provider_name,
        model=value.model,
        status=value.status,
        messages=[from_wire_agent_message(item) for item in value.messages],
        status_message=value.status_message,
        created_by_subject_id=value.created_by_subject_id,
        created_at=from_wire_timestamp(value.created_at)
        if value.HasField("created_at")
        else None,
        started_at=from_wire_timestamp(value.started_at)
        if value.HasField("started_at")
        else None,
        completed_at=from_wire_timestamp(value.completed_at)
        if value.HasField("completed_at")
        else None,
        execution_ref=value.execution_ref,
        output=from_wire_agent_turn_output(value),
    )


def to_wire_agent_turn_output(value: native.AgentTurnOutput) -> dict[str, Any]:
    if isinstance(value, native.AgentTurnText):
        return {"text": to_wire_agent_turn_text_output(value.value)}
    if isinstance(value, native.AgentTurnStructured):
        return {"structured": to_wire_agent_turn_structured_output(value.value)}
    return {}


def from_wire_agent_turn_output(value: Any) -> native.AgentTurnOutput:
    case = value.WhichOneof("output")
    if case == "text":
        return native.AgentTurnText(value=from_wire_agent_turn_text_output(value.text))
    if case == "structured":
        return native.AgentTurnStructured(
            value=from_wire_agent_turn_structured_output(value.structured)
        )
    return None


def to_wire_agent_turn_display(value: native.AgentTurnDisplay) -> Any:
    return _agent_pb2.AgentTurnDisplay(
        kind=value.kind,
        phase=value.phase,
        text=value.text,
        label=value.label,
        ref=value.ref,
        parent_ref=value.parent_ref,
        input=None if value.input is None else to_wire_value(value.input),
        output=None if value.output is None else to_wire_value(value.output),
        error=None if value.error is None else to_wire_value(value.error),
        action=value.action,
        format=value.format,
        language=value.language,
    )


def from_wire_agent_turn_display(value: Any) -> native.AgentTurnDisplay:
    return native.AgentTurnDisplay(
        kind=value.kind,
        phase=value.phase,
        text=value.text,
        label=value.label,
        ref=value.ref,
        parent_ref=value.parent_ref,
        input=from_wire_value(value.input) if value.HasField("input") else None,
        output=from_wire_value(value.output) if value.HasField("output") else None,
        error=from_wire_value(value.error) if value.HasField("error") else None,
        action=value.action,
        format=value.format,
        language=value.language,
    )


def to_wire_agent_turn_event(value: native.AgentTurnEvent) -> Any:
    return _agent_pb2.AgentTurnEvent(
        id=value.id,
        turn_id=value.turn_id,
        seq=value.seq,
        type=value.type,
        source=value.source,
        visibility=value.visibility,
        data=None if value.data is None else to_wire_struct(value.data),
        created_at=None
        if value.created_at is None
        else to_wire_timestamp(value.created_at),
        display=None
        if value.display is None
        else to_wire_agent_turn_display(value.display),
    )


def from_wire_agent_turn_event(value: Any) -> native.AgentTurnEvent:
    return native.AgentTurnEvent(
        id=value.id,
        turn_id=value.turn_id,
        seq=value.seq,
        type=value.type,
        source=value.source,
        visibility=value.visibility,
        data=from_wire_struct(value.data) if value.HasField("data") else None,
        created_at=from_wire_timestamp(value.created_at)
        if value.HasField("created_at")
        else None,
        display=from_wire_agent_turn_display(value.display)
        if value.HasField("display")
        else None,
    )


def to_wire_agent_turn_structured_output(
    value: native.AgentTurnStructuredOutput,
) -> Any:
    return _agent_pb2.AgentTurnStructuredOutput(
        text=value.text,
        value=None if value.value is None else to_wire_struct(value.value),
    )


def from_wire_agent_turn_structured_output(
    value: Any,
) -> native.AgentTurnStructuredOutput:
    return native.AgentTurnStructuredOutput(
        text=value.text,
        value=from_wire_struct(value.value) if value.HasField("value") else None,
    )


def to_wire_agent_turn_text_output(value: native.AgentTurnTextOutput) -> Any:
    return _agent_pb2.AgentTurnTextOutput(
        text=value.text,
    )


def from_wire_agent_turn_text_output(value: Any) -> native.AgentTurnTextOutput:
    return native.AgentTurnTextOutput(
        text=value.text,
    )


def to_wire_agent_workspace(value: native.AgentWorkspace) -> Any:
    return _agent_pb2.AgentWorkspace(
        checkouts=[
            to_wire_agent_workspace_git_checkout(item) for item in value.checkouts
        ],
        cwd=value.cwd,
    )


def from_wire_agent_workspace(value: Any) -> native.AgentWorkspace:
    return native.AgentWorkspace(
        checkouts=[
            from_wire_agent_workspace_git_checkout(item) for item in value.checkouts
        ],
        cwd=value.cwd,
    )


def to_wire_agent_workspace_git_checkout(
    value: native.AgentWorkspaceGitCheckout,
) -> Any:
    return _agent_pb2.AgentWorkspaceGitCheckout(
        url=value.url,
        ref=value.ref,
        path=value.path,
    )


def from_wire_agent_workspace_git_checkout(
    value: Any,
) -> native.AgentWorkspaceGitCheckout:
    return native.AgentWorkspaceGitCheckout(
        url=value.url,
        ref=value.ref,
        path=value.path,
    )


def to_wire_cancel_agent_provider_turn_request(
    value: native.CancelAgentProviderTurnRequest,
) -> Any:
    return _agent_pb2.CancelAgentProviderTurnRequest(
        turn_id=value.turn_id,
        reason=value.reason,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_cancel_agent_provider_turn_request(
    value: Any,
) -> native.CancelAgentProviderTurnRequest:
    return native.CancelAgentProviderTurnRequest(
        turn_id=value.turn_id,
        reason=value.reason,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_create_agent_provider_session_request(
    value: native.CreateAgentProviderSessionRequest,
) -> Any:
    return _agent_pb2.CreateAgentProviderSessionRequest(
        idempotency_key=value.idempotency_key,
        model=value.model,
        client_ref=value.client_ref,
        metadata=None if value.metadata is None else to_wire_struct(value.metadata),
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        session_start=None
        if value.session_start is None
        else to_wire_agent_session_start_config(value.session_start),
        prepared_workspace=None
        if value.prepared_workspace is None
        else to_wire_prepared_agent_workspace(value.prepared_workspace),
        provider_name=value.provider_name,
        workspace=None
        if value.workspace is None
        else to_wire_agent_workspace(value.workspace),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        tools=None if value.tools is None else to_wire_agent_tool_config(value.tools),
    )


def from_wire_create_agent_provider_session_request(
    value: Any,
) -> native.CreateAgentProviderSessionRequest:
    return native.CreateAgentProviderSessionRequest(
        idempotency_key=value.idempotency_key,
        model=value.model,
        client_ref=value.client_ref,
        metadata=from_wire_struct(value.metadata)
        if value.HasField("metadata")
        else None,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        session_start=from_wire_agent_session_start_config(value.session_start)
        if value.HasField("session_start")
        else None,
        prepared_workspace=from_wire_prepared_agent_workspace(value.prepared_workspace)
        if value.HasField("prepared_workspace")
        else None,
        provider_name=value.provider_name,
        workspace=from_wire_agent_workspace(value.workspace)
        if value.HasField("workspace")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        tools=from_wire_agent_tool_config(value.tools)
        if value.HasField("tools")
        else None,
    )


def to_wire_create_agent_provider_turn_request(
    value: native.CreateAgentProviderTurnRequest,
) -> Any:
    return _agent_pb2.CreateAgentProviderTurnRequest(
        turn_id=value.turn_id,
        session_id=value.session_id,
        idempotency_key=value.idempotency_key,
        model=value.model,
        messages=[to_wire_agent_message(item) for item in value.messages],
        metadata=None if value.metadata is None else to_wire_struct(value.metadata),
        execution_ref=value.execution_ref,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        model_options=None
        if value.model_options is None
        else to_wire_struct(value.model_options),
        timeout_seconds=value.timeout_seconds,
        output=None if value.output is None else to_wire_agent_output(value.output),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_create_agent_provider_turn_request(
    value: Any,
) -> native.CreateAgentProviderTurnRequest:
    return native.CreateAgentProviderTurnRequest(
        turn_id=value.turn_id,
        session_id=value.session_id,
        idempotency_key=value.idempotency_key,
        model=value.model,
        messages=[from_wire_agent_message(item) for item in value.messages],
        metadata=from_wire_struct(value.metadata)
        if value.HasField("metadata")
        else None,
        execution_ref=value.execution_ref,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        model_options=from_wire_struct(value.model_options)
        if value.HasField("model_options")
        else None,
        timeout_seconds=value.timeout_seconds,
        output=from_wire_agent_output(value.output)
        if value.HasField("output")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_get_agent_provider_capabilities_request(
    _value: native.GetAgentProviderCapabilitiesRequest,
) -> Any:
    return _agent_pb2.GetAgentProviderCapabilitiesRequest()


def from_wire_get_agent_provider_capabilities_request(
    _value: Any,
) -> native.GetAgentProviderCapabilitiesRequest:
    return native.GetAgentProviderCapabilitiesRequest()


def to_wire_get_agent_provider_interaction_request(
    value: native.GetAgentProviderInteractionRequest,
) -> Any:
    return _agent_pb2.GetAgentProviderInteractionRequest(
        interaction_id=value.interaction_id,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
    )


def from_wire_get_agent_provider_interaction_request(
    value: Any,
) -> native.GetAgentProviderInteractionRequest:
    return native.GetAgentProviderInteractionRequest(
        interaction_id=value.interaction_id,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
    )


def to_wire_get_agent_provider_session_request(
    value: native.GetAgentProviderSessionRequest,
) -> Any:
    return _agent_pb2.GetAgentProviderSessionRequest(
        session_id=value.session_id,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_get_agent_provider_session_request(
    value: Any,
) -> native.GetAgentProviderSessionRequest:
    return native.GetAgentProviderSessionRequest(
        session_id=value.session_id,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_get_agent_provider_turn_request(
    value: native.GetAgentProviderTurnRequest,
) -> Any:
    return _agent_pb2.GetAgentProviderTurnRequest(
        turn_id=value.turn_id,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_get_agent_provider_turn_request(
    value: Any,
) -> native.GetAgentProviderTurnRequest:
    return native.GetAgentProviderTurnRequest(
        turn_id=value.turn_id,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_list_agent_provider_interactions_request(
    value: native.ListAgentProviderInteractionsRequest,
) -> Any:
    return _agent_pb2.ListAgentProviderInteractionsRequest(
        turn_id=value.turn_id,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_list_agent_provider_interactions_request(
    value: Any,
) -> native.ListAgentProviderInteractionsRequest:
    return native.ListAgentProviderInteractionsRequest(
        turn_id=value.turn_id,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_list_agent_provider_interactions_response(
    value: native.ListAgentProviderInteractionsResponse,
) -> Any:
    return _agent_pb2.ListAgentProviderInteractionsResponse(
        interactions=[to_wire_agent_interaction(item) for item in value.interactions],
    )


def from_wire_list_agent_provider_interactions_response(
    value: Any,
) -> native.ListAgentProviderInteractionsResponse:
    return native.ListAgentProviderInteractionsResponse(
        interactions=[from_wire_agent_interaction(item) for item in value.interactions],
    )


def to_wire_list_agent_provider_sessions_request(
    value: native.ListAgentProviderSessionsRequest,
) -> Any:
    return _agent_pb2.ListAgentProviderSessionsRequest(
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        session_ids=value.session_ids,
        state=to_wire_enum(value.state),
        limit=value.limit,
        summary_only=value.summary_only,
        provider_name=value.provider_name,
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
    )


def from_wire_list_agent_provider_sessions_request(
    value: Any,
) -> native.ListAgentProviderSessionsRequest:
    return native.ListAgentProviderSessionsRequest(
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        session_ids=list(value.session_ids),
        state=value.state,
        limit=value.limit,
        summary_only=value.summary_only,
        provider_name=value.provider_name,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
    )


def to_wire_list_agent_provider_sessions_response(
    value: native.ListAgentProviderSessionsResponse,
) -> Any:
    return _agent_pb2.ListAgentProviderSessionsResponse(
        sessions=[to_wire_agent_session(item) for item in value.sessions],
    )


def from_wire_list_agent_provider_sessions_response(
    value: Any,
) -> native.ListAgentProviderSessionsResponse:
    return native.ListAgentProviderSessionsResponse(
        sessions=[from_wire_agent_session(item) for item in value.sessions],
    )


def to_wire_list_agent_provider_turn_events_request(
    value: native.ListAgentProviderTurnEventsRequest,
) -> Any:
    return _agent_pb2.ListAgentProviderTurnEventsRequest(
        turn_id=value.turn_id,
        after_seq=value.after_seq,
        limit=value.limit,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_list_agent_provider_turn_events_request(
    value: Any,
) -> native.ListAgentProviderTurnEventsRequest:
    return native.ListAgentProviderTurnEventsRequest(
        turn_id=value.turn_id,
        after_seq=value.after_seq,
        limit=value.limit,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_list_agent_provider_turn_events_response(
    value: native.ListAgentProviderTurnEventsResponse,
) -> Any:
    return _agent_pb2.ListAgentProviderTurnEventsResponse(
        events=[to_wire_agent_turn_event(item) for item in value.events],
    )


def from_wire_list_agent_provider_turn_events_response(
    value: Any,
) -> native.ListAgentProviderTurnEventsResponse:
    return native.ListAgentProviderTurnEventsResponse(
        events=[from_wire_agent_turn_event(item) for item in value.events],
    )


def to_wire_list_agent_provider_turns_request(
    value: native.ListAgentProviderTurnsRequest,
) -> Any:
    return _agent_pb2.ListAgentProviderTurnsRequest(
        session_id=value.session_id,
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        turn_ids=value.turn_ids,
        status=to_wire_enum(value.status),
        limit=value.limit,
        summary_only=value.summary_only,
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_list_agent_provider_turns_request(
    value: Any,
) -> native.ListAgentProviderTurnsRequest:
    return native.ListAgentProviderTurnsRequest(
        session_id=value.session_id,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        turn_ids=list(value.turn_ids),
        status=value.status,
        limit=value.limit,
        summary_only=value.summary_only,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_list_agent_provider_turns_response(
    value: native.ListAgentProviderTurnsResponse,
) -> Any:
    return _agent_pb2.ListAgentProviderTurnsResponse(
        turns=[to_wire_agent_turn(item) for item in value.turns],
    )


def from_wire_list_agent_provider_turns_response(
    value: Any,
) -> native.ListAgentProviderTurnsResponse:
    return native.ListAgentProviderTurnsResponse(
        turns=[from_wire_agent_turn(item) for item in value.turns],
    )


def to_wire_listed_agent_tool(value: native.ListedAgentTool) -> Any:
    return _agent_pb2.ListedAgentTool(
        id=value.id,
        mcp_name=value.mcp_name,
        title=value.title,
        description=value.description,
        input_schema=value.input_schema,
        output_schema=value.output_schema,
        annotations=None
        if value.annotations is None
        else app.to_wire_operation_annotations(value.annotations),
        ref=None if value.ref is None else app.to_wire_agent_tool_ref(value.ref),
        tags=value.tags,
        search_text=value.search_text,
    )


def from_wire_listed_agent_tool(value: Any) -> native.ListedAgentTool:
    return native.ListedAgentTool(
        id=value.id,
        mcp_name=value.mcp_name,
        title=value.title,
        description=value.description,
        input_schema=value.input_schema,
        output_schema=value.output_schema,
        annotations=app.from_wire_operation_annotations(value.annotations)
        if value.HasField("annotations")
        else None,
        ref=app.from_wire_agent_tool_ref(value.ref) if value.HasField("ref") else None,
        tags=list(value.tags),
        search_text=value.search_text,
    )


def to_wire_prepared_agent_workspace(value: native.PreparedAgentWorkspace) -> Any:
    return _agent_pb2.PreparedAgentWorkspace(
        root=value.root,
        cwd=value.cwd,
    )


def from_wire_prepared_agent_workspace(value: Any) -> native.PreparedAgentWorkspace:
    return native.PreparedAgentWorkspace(
        root=value.root,
        cwd=value.cwd,
    )


def to_wire_resolve_agent_provider_interaction_request(
    value: native.ResolveAgentProviderInteractionRequest,
) -> Any:
    return _agent_pb2.ResolveAgentProviderInteractionRequest(
        interaction_id=value.interaction_id,
        resolution=None
        if value.resolution is None
        else to_wire_struct(value.resolution),
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        turn_id=value.turn_id,
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_resolve_agent_provider_interaction_request(
    value: Any,
) -> native.ResolveAgentProviderInteractionRequest:
    return native.ResolveAgentProviderInteractionRequest(
        interaction_id=value.interaction_id,
        resolution=from_wire_struct(value.resolution)
        if value.HasField("resolution")
        else None,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        turn_id=value.turn_id,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )


def to_wire_update_agent_provider_session_request(
    value: native.UpdateAgentProviderSessionRequest,
) -> Any:
    return _agent_pb2.UpdateAgentProviderSessionRequest(
        session_id=value.session_id,
        client_ref=value.client_ref,
        state=to_wire_enum(value.state),
        metadata=None if value.metadata is None else to_wire_struct(value.metadata),
        subject=None
        if value.subject is None
        else app.to_wire_subject_context(value.subject),
        context=None
        if value.context is None
        else app.to_wire_request_context(value.context),
        provider_name=value.provider_name,
    )


def from_wire_update_agent_provider_session_request(
    value: Any,
) -> native.UpdateAgentProviderSessionRequest:
    return native.UpdateAgentProviderSessionRequest(
        session_id=value.session_id,
        client_ref=value.client_ref,
        state=value.state,
        metadata=from_wire_struct(value.metadata)
        if value.HasField("metadata")
        else None,
        subject=app.from_wire_subject_context(value.subject)
        if value.HasField("subject")
        else None,
        context=app.from_wire_request_context(value.context)
        if value.HasField("context")
        else None,
        provider_name=value.provider_name,
    )

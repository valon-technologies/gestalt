# Code generated by sdkgen. DO NOT EDIT.

"""Generated wire codec for remote.proto."""

from __future__ import annotations

from typing import Any

from .. import remote as native
from .._gen.v1 import remote_pb2 as _remote_pb2
from .support import (
    from_wire_duration,
    from_wire_struct,
    from_wire_timestamp,
    to_wire_duration,
    to_wire_struct,
    to_wire_timestamp,
)


def to_wire_create_remote_request(value: native.CreateRemoteRequest) -> Any:
    return _remote_pb2.CreateRemoteRequest(
        tunnel=None if value.tunnel is None else to_wire_tunnel_endpoint(value.tunnel),
        providers=[
            to_wire_remote_provider_definition(item) for item in value.providers
        ],
        expected_generation=value.expected_generation,
    )


def from_wire_create_remote_request(value: Any) -> native.CreateRemoteRequest:
    return native.CreateRemoteRequest(
        tunnel=from_wire_tunnel_endpoint(value.tunnel)
        if value.HasField("tunnel")
        else None,
        providers=[
            from_wire_remote_provider_definition(item) for item in value.providers
        ],
        expected_generation=value.expected_generation,
    )


def to_wire_delete_remote_request(value: native.DeleteRemoteRequest) -> Any:
    return _remote_pb2.DeleteRemoteRequest(
        id=value.id,
        expected_generation=value.expected_generation,
    )


def from_wire_delete_remote_request(value: Any) -> native.DeleteRemoteRequest:
    return native.DeleteRemoteRequest(
        id=value.id,
        expected_generation=value.expected_generation,
    )


def to_wire_list_remotes_request(_value: native.ListRemotesRequest) -> Any:
    return _remote_pb2.ListRemotesRequest()


def from_wire_list_remotes_request(_value: Any) -> native.ListRemotesRequest:
    return native.ListRemotesRequest()


def to_wire_list_remotes_response(value: native.ListRemotesResponse) -> Any:
    return _remote_pb2.ListRemotesResponse(
        remotes=[to_wire_remote(item) for item in value.remotes],
        server_identity=None
        if value.server_identity is None
        else to_wire_server_identity(value.server_identity),
        tunnel=None if value.tunnel is None else to_wire_tunnel_bootstrap(value.tunnel),
        lease_duration=None
        if value.lease_duration is None
        else to_wire_duration(value.lease_duration),
    )


def from_wire_list_remotes_response(value: Any) -> native.ListRemotesResponse:
    return native.ListRemotesResponse(
        remotes=[from_wire_remote(item) for item in value.remotes],
        server_identity=from_wire_server_identity(value.server_identity)
        if value.HasField("server_identity")
        else None,
        tunnel=from_wire_tunnel_bootstrap(value.tunnel)
        if value.HasField("tunnel")
        else None,
        lease_duration=from_wire_duration(value.lease_duration)
        if value.HasField("lease_duration")
        else None,
    )


def to_wire_provider_ref(value: native.ProviderRef) -> Any:
    return _remote_pb2.ProviderRef(
        kind=value.kind,
        name=value.name,
    )


def from_wire_provider_ref(value: Any) -> native.ProviderRef:
    return native.ProviderRef(
        kind=value.kind,
        name=value.name,
    )


def to_wire_registration_check_request(value: native.RegistrationCheckRequest) -> Any:
    return _remote_pb2.RegistrationCheckRequest(
        registration_id=value.registration_id,
        generation=value.generation,
        providers=[to_wire_provider_ref(item) for item in value.providers],
    )


def from_wire_registration_check_request(value: Any) -> native.RegistrationCheckRequest:
    return native.RegistrationCheckRequest(
        registration_id=value.registration_id,
        generation=value.generation,
        providers=[from_wire_provider_ref(item) for item in value.providers],
    )


def to_wire_registration_check_response(value: native.RegistrationCheckResponse) -> Any:
    return _remote_pb2.RegistrationCheckResponse(
        ready=value.ready,
        message=value.message,
    )


def from_wire_registration_check_response(
    value: Any,
) -> native.RegistrationCheckResponse:
    return native.RegistrationCheckResponse(
        ready=value.ready,
        message=value.message,
    )


def to_wire_remote(value: native.Remote) -> Any:
    return _remote_pb2.Remote(
        id=value.id,
        owner_subject_id=value.owner_subject_id,
        generation=value.generation,
        providers=[to_wire_remote_provider_summary(item) for item in value.providers],
        reachability=None
        if value.reachability is None
        else to_wire_remote_reachability(value.reachability),
        server_spki_sha256=value.server_spki_sha256,
        created_at=None
        if value.created_at is None
        else to_wire_timestamp(value.created_at),
        updated_at=None
        if value.updated_at is None
        else to_wire_timestamp(value.updated_at),
        last_checked_at=None
        if value.last_checked_at is None
        else to_wire_timestamp(value.last_checked_at),
        last_successful_heartbeat_at=None
        if value.last_successful_heartbeat_at is None
        else to_wire_timestamp(value.last_successful_heartbeat_at),
        last_error=value.last_error,
        lease_expires_at=None
        if value.lease_expires_at is None
        else to_wire_timestamp(value.lease_expires_at),
        connect_url=value.connect_url,
    )


def from_wire_remote(value: Any) -> native.Remote:
    return native.Remote(
        id=value.id,
        owner_subject_id=value.owner_subject_id,
        generation=value.generation,
        providers=[from_wire_remote_provider_summary(item) for item in value.providers],
        reachability=from_wire_remote_reachability(value.reachability)
        if value.HasField("reachability")
        else None,
        server_spki_sha256=value.server_spki_sha256,
        created_at=from_wire_timestamp(value.created_at)
        if value.HasField("created_at")
        else None,
        updated_at=from_wire_timestamp(value.updated_at)
        if value.HasField("updated_at")
        else None,
        last_checked_at=from_wire_timestamp(value.last_checked_at)
        if value.HasField("last_checked_at")
        else None,
        last_successful_heartbeat_at=from_wire_timestamp(
            value.last_successful_heartbeat_at
        )
        if value.HasField("last_successful_heartbeat_at")
        else None,
        last_error=value.last_error,
        lease_expires_at=from_wire_timestamp(value.lease_expires_at)
        if value.HasField("lease_expires_at")
        else None,
        connect_url=value.connect_url,
    )


def to_wire_remote_provider_definition(value: native.RemoteProviderDefinition) -> Any:
    return _remote_pb2.RemoteProviderDefinition(
        kind=value.kind,
        name=value.name,
        definition=None
        if value.definition is None
        else to_wire_struct(value.definition),
    )


def from_wire_remote_provider_definition(value: Any) -> native.RemoteProviderDefinition:
    return native.RemoteProviderDefinition(
        kind=value.kind,
        name=value.name,
        definition=from_wire_struct(value.definition)
        if value.HasField("definition")
        else None,
    )


def to_wire_remote_provider_summary(value: native.RemoteProviderSummary) -> Any:
    return _remote_pb2.RemoteProviderSummary(
        kind=value.kind,
        name=value.name,
    )


def from_wire_remote_provider_summary(value: Any) -> native.RemoteProviderSummary:
    return native.RemoteProviderSummary(
        kind=value.kind,
        name=value.name,
    )


def to_wire_remote_reachability(value: native.RemoteReachability) -> Any:
    return _remote_pb2.RemoteReachability(
        reachable=value.reachable,
        message=value.message,
    )


def from_wire_remote_reachability(value: Any) -> native.RemoteReachability:
    return native.RemoteReachability(
        reachable=value.reachable,
        message=value.message,
    )


def to_wire_server_identity(value: native.ServerIdentity) -> Any:
    return _remote_pb2.ServerIdentity(
        client_spki_sha256=value.client_spki_sha256,
    )


def from_wire_server_identity(value: Any) -> native.ServerIdentity:
    return native.ServerIdentity(
        client_spki_sha256=value.client_spki_sha256,
    )


def to_wire_tunnel_bootstrap(value: native.TunnelBootstrap) -> Any:
    return _remote_pb2.TunnelBootstrap(
        frps_address=value.frps_address,
        lease_duration=None
        if value.lease_duration is None
        else to_wire_duration(value.lease_duration),
    )


def from_wire_tunnel_bootstrap(value: Any) -> native.TunnelBootstrap:
    return native.TunnelBootstrap(
        frps_address=value.frps_address,
        lease_duration=from_wire_duration(value.lease_duration)
        if value.HasField("lease_duration")
        else None,
    )


def to_wire_tunnel_endpoint(value: native.TunnelEndpoint) -> Any:
    return _remote_pb2.TunnelEndpoint(
        host=value.host,
        certificate=value.certificate,
        server_spki_sha256=value.server_spki_sha256,
    )


def from_wire_tunnel_endpoint(value: Any) -> native.TunnelEndpoint:
    return native.TunnelEndpoint(
        host=value.host,
        certificate=value.certificate,
        server_spki_sha256=value.server_spki_sha256,
    )

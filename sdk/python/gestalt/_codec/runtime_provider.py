# Code generated by sdkgen. DO NOT EDIT.

"""Generated wire codec for runtime_provider.proto."""

from __future__ import annotations

from typing import Any

from .. import runtime_provider as native
from .._gen.v1 import runtime_provider_pb2 as _runtime_provider_pb2
from . import agent
from .support import from_wire_timestamp, to_wire_enum, to_wire_timestamp


def to_wire_append_runtime_logs_request(value: native.AppendRuntimeLogsRequest) -> Any:
    return _runtime_provider_pb2.AppendRuntimeLogsRequest(
        session_id=value.session_id,
        logs=[to_wire_runtime_log_entry(item) for item in value.logs],
    )


def from_wire_append_runtime_logs_request(
    value: Any,
) -> native.AppendRuntimeLogsRequest:
    return native.AppendRuntimeLogsRequest(
        session_id=value.session_id,
        logs=[from_wire_runtime_log_entry(item) for item in value.logs],
    )


def to_wire_append_runtime_logs_response(
    value: native.AppendRuntimeLogsResponse,
) -> Any:
    return _runtime_provider_pb2.AppendRuntimeLogsResponse(
        last_seq=value.last_seq,
    )


def from_wire_append_runtime_logs_response(
    value: Any,
) -> native.AppendRuntimeLogsResponse:
    return native.AppendRuntimeLogsResponse(
        last_seq=value.last_seq,
    )


def to_wire_get_runtime_session_request(value: native.GetRuntimeSessionRequest) -> Any:
    return _runtime_provider_pb2.GetRuntimeSessionRequest(
        session_id=value.session_id,
    )


def from_wire_get_runtime_session_request(
    value: Any,
) -> native.GetRuntimeSessionRequest:
    return native.GetRuntimeSessionRequest(
        session_id=value.session_id,
    )


def to_wire_hosted_app(value: native.HostedApp) -> Any:
    return _runtime_provider_pb2.HostedApp(
        id=value.id,
        session_id=value.session_id,
        app_name=value.app_name,
        dial_target=value.dial_target,
    )


def from_wire_hosted_app(value: Any) -> native.HostedApp:
    return native.HostedApp(
        id=value.id,
        session_id=value.session_id,
        app_name=value.app_name,
        dial_target=value.dial_target,
    )


def to_wire_list_runtime_sessions_request(
    value: native.ListRuntimeSessionsRequest,
) -> Any:
    return _runtime_provider_pb2.ListRuntimeSessionsRequest(
        page_size=value.page_size,
        page_token=value.page_token,
    )


def from_wire_list_runtime_sessions_request(
    value: Any,
) -> native.ListRuntimeSessionsRequest:
    return native.ListRuntimeSessionsRequest(
        page_size=value.page_size,
        page_token=value.page_token,
    )


def to_wire_list_runtime_sessions_response(
    value: native.ListRuntimeSessionsResponse,
) -> Any:
    return _runtime_provider_pb2.ListRuntimeSessionsResponse(
        sessions=[to_wire_runtime_session(item) for item in value.sessions],
        next_page_token=value.next_page_token,
    )


def from_wire_list_runtime_sessions_response(
    value: Any,
) -> native.ListRuntimeSessionsResponse:
    return native.ListRuntimeSessionsResponse(
        sessions=[from_wire_runtime_session(item) for item in value.sessions],
        next_page_token=value.next_page_token,
    )


def to_wire_prepare_runtime_workspace_request(
    value: native.PrepareRuntimeWorkspaceRequest,
) -> Any:
    return _runtime_provider_pb2.PrepareRuntimeWorkspaceRequest(
        session_id=value.session_id,
        agent_session_id=value.agent_session_id,
        workspace=None
        if value.workspace is None
        else agent.to_wire_agent_workspace(value.workspace),
    )


def from_wire_prepare_runtime_workspace_request(
    value: Any,
) -> native.PrepareRuntimeWorkspaceRequest:
    return native.PrepareRuntimeWorkspaceRequest(
        session_id=value.session_id,
        agent_session_id=value.agent_session_id,
        workspace=agent.from_wire_agent_workspace(value.workspace)
        if value.HasField("workspace")
        else None,
    )


def to_wire_prepare_runtime_workspace_response(
    value: native.PrepareRuntimeWorkspaceResponse,
) -> Any:
    return _runtime_provider_pb2.PrepareRuntimeWorkspaceResponse(
        workspace=None
        if value.workspace is None
        else agent.to_wire_prepared_agent_workspace(value.workspace),
    )


def from_wire_prepare_runtime_workspace_response(
    value: Any,
) -> native.PrepareRuntimeWorkspaceResponse:
    return native.PrepareRuntimeWorkspaceResponse(
        workspace=agent.from_wire_prepared_agent_workspace(value.workspace)
        if value.HasField("workspace")
        else None,
    )


def to_wire_remove_runtime_workspace_request(
    value: native.RemoveRuntimeWorkspaceRequest,
) -> Any:
    return _runtime_provider_pb2.RemoveRuntimeWorkspaceRequest(
        session_id=value.session_id,
        agent_session_id=value.agent_session_id,
    )


def from_wire_remove_runtime_workspace_request(
    value: Any,
) -> native.RemoveRuntimeWorkspaceRequest:
    return native.RemoveRuntimeWorkspaceRequest(
        session_id=value.session_id,
        agent_session_id=value.agent_session_id,
    )


def to_wire_runtime_image_pull_auth(value: native.RuntimeImagePullAuth) -> Any:
    return _runtime_provider_pb2.RuntimeImagePullAuth(
        docker_config_json=value.docker_config_json,
    )


def from_wire_runtime_image_pull_auth(value: Any) -> native.RuntimeImagePullAuth:
    return native.RuntimeImagePullAuth(
        docker_config_json=value.docker_config_json,
    )


def to_wire_runtime_log_entry(value: native.RuntimeLogEntry) -> Any:
    return _runtime_provider_pb2.RuntimeLogEntry(
        stream=to_wire_enum(value.stream),
        message=value.message,
        observed_at=None
        if value.observed_at is None
        else to_wire_timestamp(value.observed_at),
        source_seq=value.source_seq,
    )


def from_wire_runtime_log_entry(value: Any) -> native.RuntimeLogEntry:
    return native.RuntimeLogEntry(
        stream=value.stream,
        message=value.message,
        observed_at=from_wire_timestamp(value.observed_at)
        if value.HasField("observed_at")
        else None,
        source_seq=value.source_seq,
    )


def to_wire_runtime_session(value: native.RuntimeSession) -> Any:
    return _runtime_provider_pb2.RuntimeSession(
        id=value.id,
        state=value.state,
        metadata=value.metadata,
        lifecycle=None
        if value.lifecycle is None
        else to_wire_runtime_session_lifecycle(value.lifecycle),
        state_reason=value.state_reason,
        state_message=value.state_message,
    )


def from_wire_runtime_session(value: Any) -> native.RuntimeSession:
    return native.RuntimeSession(
        id=value.id,
        state=value.state,
        metadata=dict(value.metadata),
        lifecycle=from_wire_runtime_session_lifecycle(value.lifecycle)
        if value.HasField("lifecycle")
        else None,
        state_reason=value.state_reason,
        state_message=value.state_message,
    )


def to_wire_runtime_session_lifecycle(value: native.RuntimeSessionLifecycle) -> Any:
    return _runtime_provider_pb2.RuntimeSessionLifecycle(
        started_at=None
        if value.started_at is None
        else to_wire_timestamp(value.started_at),
        recommended_drain_at=None
        if value.recommended_drain_at is None
        else to_wire_timestamp(value.recommended_drain_at),
        expires_at=None
        if value.expires_at is None
        else to_wire_timestamp(value.expires_at),
    )


def from_wire_runtime_session_lifecycle(value: Any) -> native.RuntimeSessionLifecycle:
    return native.RuntimeSessionLifecycle(
        started_at=from_wire_timestamp(value.started_at)
        if value.HasField("started_at")
        else None,
        recommended_drain_at=from_wire_timestamp(value.recommended_drain_at)
        if value.HasField("recommended_drain_at")
        else None,
        expires_at=from_wire_timestamp(value.expires_at)
        if value.HasField("expires_at")
        else None,
    )


def to_wire_runtime_support(value: native.RuntimeSupport) -> Any:
    return _runtime_provider_pb2.RuntimeSupport(
        can_host_apps=value.can_host_apps,
        egress_mode=to_wire_enum(value.egress_mode),
        supports_prepare_workspace=value.supports_prepare_workspace,
    )


def from_wire_runtime_support(value: Any) -> native.RuntimeSupport:
    return native.RuntimeSupport(
        can_host_apps=value.can_host_apps,
        egress_mode=value.egress_mode,
        supports_prepare_workspace=value.supports_prepare_workspace,
    )


def to_wire_start_hosted_app_request(value: native.StartHostedAppRequest) -> Any:
    return _runtime_provider_pb2.StartHostedAppRequest(
        session_id=value.session_id,
        app_name=value.app_name,
        command=value.command,
        args=value.args,
        env=value.env,
        allowed_hosts=value.allowed_hosts,
        default_action=value.default_action,
        host_binary=value.host_binary,
        workdir=value.workdir,
    )


def from_wire_start_hosted_app_request(value: Any) -> native.StartHostedAppRequest:
    return native.StartHostedAppRequest(
        session_id=value.session_id,
        app_name=value.app_name,
        command=value.command,
        args=list(value.args),
        env=dict(value.env),
        allowed_hosts=list(value.allowed_hosts),
        default_action=value.default_action,
        host_binary=value.host_binary,
        workdir=value.workdir,
    )


def to_wire_start_runtime_session_request(
    value: native.StartRuntimeSessionRequest,
) -> Any:
    return _runtime_provider_pb2.StartRuntimeSessionRequest(
        app_name=value.app_name,
        template=value.template,
        image=value.image,
        metadata=value.metadata,
        image_pull_auth=None
        if value.image_pull_auth is None
        else to_wire_runtime_image_pull_auth(value.image_pull_auth),
    )


def from_wire_start_runtime_session_request(
    value: Any,
) -> native.StartRuntimeSessionRequest:
    return native.StartRuntimeSessionRequest(
        app_name=value.app_name,
        template=value.template,
        image=value.image,
        metadata=dict(value.metadata),
        image_pull_auth=from_wire_runtime_image_pull_auth(value.image_pull_auth)
        if value.HasField("image_pull_auth")
        else None,
    )


def to_wire_stop_runtime_session_request(
    value: native.StopRuntimeSessionRequest,
) -> Any:
    return _runtime_provider_pb2.StopRuntimeSessionRequest(
        session_id=value.session_id,
    )


def from_wire_stop_runtime_session_request(
    value: Any,
) -> native.StopRuntimeSessionRequest:
    return native.StopRuntimeSessionRequest(
        session_id=value.session_id,
    )

# Code generated by sdkgen. DO NOT EDIT.

"""Schema-derived, I/O-neutral public REST transport kernel."""

from __future__ import annotations

import base64
import json
import re
from dataclasses import dataclass
from typing import Any, TypeVar, cast
from urllib.parse import quote

from google.protobuf import json_format
from google.protobuf.message import Message

from gestalt.rpc_support import GestaltError, GestaltErrorCode

from .metadata import Method, PublicField

ResponseT = TypeVar("ResponseT", bound=Message)

RESPONSE_KIND_HEADER = "X-Gestalt-Response-Kind"
RESPONSE_KIND_OPERATION_RESULT = "operation-result"

_HTTP_STATUS_CODES = {
    400: GestaltErrorCode.INVALID_ARGUMENT,
    401: GestaltErrorCode.UNAUTHENTICATED,
    403: GestaltErrorCode.PERMISSION_DENIED,
    404: GestaltErrorCode.NOT_FOUND,
    409: GestaltErrorCode.ALREADY_EXISTS,
    412: GestaltErrorCode.FAILED_PRECONDITION,
    429: GestaltErrorCode.RESOURCE_EXHAUSTED,
    499: GestaltErrorCode.CANCELED,
    500: GestaltErrorCode.INTERNAL,
    501: GestaltErrorCode.UNIMPLEMENTED,
    503: GestaltErrorCode.UNAVAILABLE,
    504: GestaltErrorCode.DEADLINE_EXCEEDED,
}

_GESTALT_CODE_NAMES = {
    "CANCELED": GestaltErrorCode.CANCELED,
    "CANCELLED": GestaltErrorCode.CANCELED,
    "UNKNOWN": GestaltErrorCode.UNKNOWN,
    "INVALID_ARGUMENT": GestaltErrorCode.INVALID_ARGUMENT,
    "DEADLINE_EXCEEDED": GestaltErrorCode.DEADLINE_EXCEEDED,
    "NOT_FOUND": GestaltErrorCode.NOT_FOUND,
    "ALREADY_EXISTS": GestaltErrorCode.ALREADY_EXISTS,
    "PERMISSION_DENIED": GestaltErrorCode.PERMISSION_DENIED,
    "RESOURCE_EXHAUSTED": GestaltErrorCode.RESOURCE_EXHAUSTED,
    "FAILED_PRECONDITION": GestaltErrorCode.FAILED_PRECONDITION,
    "ABORTED": GestaltErrorCode.ABORTED,
    "OUT_OF_RANGE": GestaltErrorCode.OUT_OF_RANGE,
    "UNIMPLEMENTED": GestaltErrorCode.UNIMPLEMENTED,
    "INTERNAL": GestaltErrorCode.INTERNAL,
    "UNAVAILABLE": GestaltErrorCode.UNAVAILABLE,
    "DATA_LOSS": GestaltErrorCode.DATA_LOSS,
    "UNAUTHENTICATED": GestaltErrorCode.UNAUTHENTICATED,
}


@dataclass(frozen=True, slots=True)
class PreparedRestRequest:
    verb: str
    path: str
    query: tuple[tuple[str, str], ...]
    body: dict[str, Any] | None


@dataclass(frozen=True, slots=True)
class RawRestResponse:
    status: int
    headers: tuple[tuple[str, str], ...]
    body: bytes


def prepare_rest_request(method: Method, request: Message) -> PreparedRestRequest:
    if not method.http_verb or not method.http_path:
        raise GestaltError(
            GestaltErrorCode.INVALID_ARGUMENT,
            f"method {method.full_method} has no HTTP binding",
        )
    request_json: dict[str, Any] = json_format.MessageToDict(
        request,
        preserving_proto_field_name=False,
    )
    path = build_rest_path(method, request_json)
    query = tuple(build_rest_query(method, request_json))
    body = build_rest_body(method, request_json)
    return PreparedRestRequest(
        verb=method.http_verb,
        path=path,
        query=query,
        body=body,
    )


def decode_rest_response(
    method: Method,
    response_type: type[ResponseT],
    response: RawRestResponse,
) -> ResponseT:
    if is_operation_result(response.headers):
        if not method.response_is_operation_result:
            raise GestaltError(
                GestaltErrorCode.INTERNAL,
                "unexpected operation-result response kind",
            )
        response_json = synthesize_operation_result_json(
            response.status,
            response.body,
            response.headers,
        )
    elif response.status >= 400:
        raise parse_gateway_error(response.status, response.body)
    elif not response.body.strip():
        response_json: dict[str, Any] = {}
    else:
        try:
            parsed = json.loads(response.body)
        except UnicodeDecodeError as err:
            raise GestaltError(
                GestaltErrorCode.INTERNAL,
                f"invalid UTF-8 in response: {err}",
            ) from err
        except json.JSONDecodeError as err:
            raise GestaltError(
                GestaltErrorCode.INTERNAL,
                f"invalid JSON response: {err}",
            ) from err
        if not isinstance(parsed, dict):
            raise GestaltError(
                GestaltErrorCode.INTERNAL,
                "expected JSON object response",
            )
        response_json = parsed

    message = response_type()
    try:
        json_format.ParseDict(response_json, message)
    except Exception as err:
        raise GestaltError(
            GestaltErrorCode.INTERNAL,
            f"response does not match expected schema: {err}",
        ) from err
    return message


def parse_gateway_error(status: int, body: bytes) -> GestaltError:
    code = _HTTP_STATUS_CODES.get(status, GestaltErrorCode.UNKNOWN)
    message = f"request failed with status {status}"
    if not body.strip():
        return GestaltError(code, message)

    try:
        payload = json.loads(body)
    except UnicodeDecodeError:
        return GestaltError(code, message)
    except json.JSONDecodeError:
        return GestaltError(code, message)

    if not isinstance(payload, dict):
        return GestaltError(code, message)

    parsed_code, parsed_message = _gateway_fields(payload)
    if parsed_message:
        message = parsed_message
    if parsed_code is not None:
        code = parsed_code
    return GestaltError(code, message)


def snake_to_camel(name: str) -> str:
    parts = name.split("_")
    if not parts:
        return name
    head, *tail = parts
    return head + "".join(part[:1].upper() + part[1:] for part in tail if part)


def field_value(
    request: dict[str, Any],
    field_name: str,
    json_name: str,
) -> Any:
    if json_name in request:
        return request[json_name]
    if field_name in request:
        return request[field_name]
    camel = snake_to_camel(field_name)
    if camel in request:
        return request[camel]
    return None


def excluded_field_keys(method: Method) -> set[str]:
    keys: set[str] = set()
    for field in method.http_path_fields:
        keys.update(_field_aliases(field))
    for field in method.http_query_fields:
        keys.update(_field_aliases(field))
    for name in (*method.fill, *method.reject):
        keys.add(name)
        keys.add(snake_to_camel(name))
    return keys


def build_rest_path(method: Method, request: dict[str, Any]) -> str:
    path = method.http_path
    for field in method.http_path_fields:
        value = field_value(request, field.name, field.json_name)
        if value is None:
            raise GestaltError(
                GestaltErrorCode.INVALID_ARGUMENT,
                f"missing path parameter {field.name}",
            )
        if isinstance(value, (dict, list)):
            raise GestaltError(
                GestaltErrorCode.INVALID_ARGUMENT,
                f"path parameter {field.name} must be scalar",
            )
        path = path.replace(
            f"{{{field.name}}}",
            quote(str(value), safe=""),
        )
    return path


def append_query_params(
    pairs: list[tuple[str, str]],
    prefix: str,
    value: Any,
) -> None:
    if value is None:
        return
    if isinstance(value, list):
        for item in value:
            if item is None:
                continue
            if isinstance(item, (dict, list)):
                raise GestaltError(
                    GestaltErrorCode.INVALID_ARGUMENT,
                    f"repeated query field {prefix} must contain scalars",
                )
            pairs.append((prefix, _format_query_scalar(item)))
        return
    if isinstance(value, dict):
        for key, nested in value.items():
            nested_prefix = f"{prefix}.{key}" if prefix else key
            append_query_params(pairs, nested_prefix, nested)
        return
    pairs.append((prefix, _format_query_scalar(value)))


def build_rest_query(method: Method, request: dict[str, Any]) -> list[tuple[str, str]]:
    pairs: list[tuple[str, str]] = []
    for field in method.http_query_fields:
        value = field_value(request, field.name, field.json_name)
        if value is None:
            continue
        append_query_params(pairs, field.json_name, value)
    return pairs


def build_rest_body(
    method: Method,
    request: dict[str, Any],
) -> dict[str, Any] | None:
    if method.http_body == "*":
        excluded = excluded_field_keys(method)
        body: dict[str, Any] = {}
        for key, value in request.items():
            if key in excluded or value is None:
                continue
            body[key] = value
        return body
    if not method.http_body:
        return None
    value = field_value(request, method.http_body, snake_to_camel(method.http_body))
    if value is None:
        value = request.get(method.http_body)
    if value is None:
        return {}
    if isinstance(value, dict):
        return value
    return {method.http_body: value}


def is_operation_result(headers: tuple[tuple[str, str], ...]) -> bool:
    for key, value in headers:
        if key.lower() == RESPONSE_KIND_HEADER.lower():
            if value.lower() == RESPONSE_KIND_OPERATION_RESULT:
                return True
    return False


def synthesize_operation_result_json(
    status: int,
    body: bytes,
    headers: tuple[tuple[str, str], ...],
) -> dict[str, Any]:
    grouped: dict[str, list[str]] = {}
    for key, value in headers:
        if key.lower() == RESPONSE_KIND_HEADER.lower():
            continue
        grouped.setdefault(key, []).append(value)
    header_json = {key: {"values": values} for key, values in grouped.items()}
    return {
        "status": status,
        "body": base64.b64encode(body).decode("ascii"),
        "headers": header_json,
    }


def _field_aliases(field: PublicField) -> set[str]:
    return {field.name, field.json_name, snake_to_camel(field.name)}


def _format_query_scalar(value: Any) -> str:
    if isinstance(value, bool):
        return "true" if value else "false"
    return str(value)


def _gateway_fields(payload: dict[str, object]) -> tuple[int | None, str | None]:
    message: str | None = None
    code: int | None = None

    raw_message = payload.get("message")
    if isinstance(raw_message, str) and raw_message.strip():
        message = raw_message

    error = payload.get("error")
    if isinstance(error, str) and error.strip():
        message = error
    elif isinstance(error, dict):
        error_payload = cast(dict[str, object], error)
        nested_message = error_payload.get("message")
        if isinstance(nested_message, str) and nested_message.strip():
            message = nested_message
        nested_code = error_payload.get("code")
        if isinstance(nested_code, int):
            code = nested_code
        elif isinstance(nested_code, str) and nested_code.strip():
            if (parsed := _gestalt_code_from_string(nested_code)) is not None:
                code = parsed

    raw_code = payload.get("code")
    if isinstance(raw_code, int):
        code = raw_code
    elif isinstance(raw_code, str) and raw_code.strip():
        if (parsed := _gestalt_code_from_string(raw_code)) is not None:
            code = parsed

    return code, message


def _gestalt_code_from_string(raw: str) -> int | None:
    stripped = raw.strip()
    if stripped.isdigit():
        return int(stripped)
    direct = stripped.upper().replace("-", "_")
    if direct in _GESTALT_CODE_NAMES:
        return _GESTALT_CODE_NAMES[direct]
    normalized = re.sub(r"(?<!^)(?=[A-Z])", "_", stripped).upper()
    normalized = normalized.replace("-", "_")
    return _GESTALT_CODE_NAMES.get(normalized)

// Code generated by sdkgen. DO NOT EDIT.

package client

import (
	"context"

	"github.com/valon-technologies/gestalt/sdk/go/internal/host"
	proto "github.com/valon-technologies/gestalt/server/rpc/protov1/v1"
	"google.golang.org/grpc"
)

// ApplyWorkflowMigrationRequest is the native message type for gestalt.provider.v1.ApplyWorkflowMigrationRequest.
//
// ApplyWorkflowMigrationRequest applies one workflow-definition migration revision
// on behalf of a configuring app provider. Only callable while the caller is in
// the trusted configure/migration phase.
type ApplyWorkflowMigrationRequest struct {
	// Workflow provider name. Must be explicit; "default" is rejected.
	Provider string
	// Migration revision id recorded in the app ledger after success.
	RevisionId string
	// Raw workflow definition spec materialized by the app migration.
	Spec *WorkflowDefinitionSpec
	// Stable idempotency key derived from migration identity and definition fingerprint.
	IdempotencyKey string
}

// ApplyWorkflowMigrationResponse is the native message type for gestalt.provider.v1.ApplyWorkflowMigrationResponse.
type ApplyWorkflowMigrationResponse struct {
	Definition *WorkflowDefinition
}

// Migration is the generated client for gestalt.provider.v1.Migration.
// Every transport error is converted to *GestaltError.
//
// Migration is a host service available to provider processes during configure.
type Migration struct {
	client proto.MigrationClient
}

// NewMigration creates a Migration client over an injected gRPC connection.
func NewMigration(conn grpc.ClientConnInterface) *Migration {
	return &Migration{client: proto.NewMigrationClient(conn)}
}

var connectMigrationConns host.ConnPool

// ConnectMigration dials the "migrations" host service advertised through the
// GESTALT_HOST_SERVICE_SOCKET environment and returns a connected client.
// name selects a named binding; the empty string selects the default
// binding. Connections are pooled per binding and shared across clients for
// the life of the process. The first dial blocks until the connection is
// ready or ctx is done.
func ConnectMigration(ctx context.Context, name string) (*Migration, error) {
	target, token, err := host.Target("migrations")
	if err != nil {
		return nil, toGestaltError(err)
	}
	conn, err := connectMigrationConns.Conn(ctx, "migrations", target, token, name)
	if err != nil {
		return nil, toGestaltError(err)
	}
	return NewMigration(conn), nil
}

// ApplyWorkflowMigration is the ergonomic form of [Migration.ApplyWorkflowMigrationRaw].
// The response collapses to its definition field.
func (c *Migration) ApplyWorkflowMigration(ctx context.Context, request *ApplyWorkflowMigrationRequest) (*WorkflowDefinition, error) {
	response, err := c.client.ApplyWorkflowMigration(ctx, ToWireApplyWorkflowMigrationRequest(request))
	if err != nil {
		return nil, toGestaltError(err)
	}
	return FromWireApplyWorkflowMigrationResponse(response).Definition, nil
}

// ApplyWorkflowMigrationRaw is the faithful form of [Migration.ApplyWorkflowMigration].
func (c *Migration) ApplyWorkflowMigrationRaw(ctx context.Context, request *ApplyWorkflowMigrationRequest) (*ApplyWorkflowMigrationResponse, error) {
	response, err := c.client.ApplyWorkflowMigration(ctx, ToWireApplyWorkflowMigrationRequest(request))
	if err != nil {
		return nil, toGestaltError(err)
	}
	return FromWireApplyWorkflowMigrationResponse(response), nil
}

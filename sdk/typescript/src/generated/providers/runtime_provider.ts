// Code generated by sdkgen. DO NOT EDIT.

/**
 * Generated provider handler base and service adapter for runtime_provider.proto.
 *
 * @module providers/runtime_provider
 */

import { ConnectError, Code, type ServiceImpl } from "@connectrpc/connect";
import * as wire from "../../internal/gen/v1/runtime_provider_pb.ts";
import {
  fromWireGetRuntimeSessionRequest,
  fromWireListRuntimeSessionsRequest,
  fromWirePrepareRuntimeWorkspaceRequest,
  fromWireRemoveRuntimeWorkspaceRequest,
  fromWireStartHostedAppRequest,
  fromWireStartRuntimeSessionRequest,
  fromWireStopRuntimeSessionRequest,
  toWireHostedApp,
  toWireListRuntimeSessionsResponse,
  toWirePrepareRuntimeWorkspaceResponse,
  toWireRuntimeSession,
  toWireRuntimeSupport,
} from "../../internal/codec/runtime_provider.ts";
import { type PreparedAgentWorkspace } from "../../agent.ts";
import {
  type GetRuntimeSessionRequest,
  type HostedApp,
  type ListRuntimeSessionsRequest,
  type ListRuntimeSessionsResponse,
  type PrepareRuntimeWorkspaceRequest,
  type RemoveRuntimeWorkspaceRequest,
  type RuntimeSession,
  type RuntimeSupport,
  type StartHostedAppRequest,
  type StartRuntimeSessionRequest,
  type StopRuntimeSessionRequest,
} from "../../runtime_provider.ts";
import { GestaltError, GestaltErrorCode } from "../../rpc_support.ts";
import { ProviderBase } from "../../provider-base.ts";

// statusError converts one handler error to a ConnectError for the host.
function statusError(operation: string, error: unknown): ConnectError {
  if (error instanceof ConnectError) {
    return error;
  }
  if (error instanceof GestaltError) {
    return new ConnectError(error.message, error.code as Code);
  }
  return new ConnectError(
    `${operation}: ${error instanceof Error ? error.message : String(error)}`,
    Code.Unknown,
  );
}

/**
 * RuntimeProvider is the base handler class for the gestalt.provider.v1.Runtime service.
 *
 * Extend this class and override the methods your provider implements.
 * Unoverridden methods throw a GestaltError with code Unimplemented.
 */
export abstract class RuntimeProvider extends ProviderBase {
  readonly kind = "runtime" as const;

  async getSupport(): Promise<RuntimeSupport> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime get support is not implemented",
    );
  }

  async startSession(
    request: StartRuntimeSessionRequest,
  ): Promise<RuntimeSession> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime start session is not implemented",
    );
  }

  async getSession(request: GetRuntimeSessionRequest): Promise<RuntimeSession> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime get session is not implemented",
    );
  }

  async listSessions(
    request: ListRuntimeSessionsRequest,
  ): Promise<ListRuntimeSessionsResponse> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime list sessions is not implemented",
    );
  }

  async stopSession(request: StopRuntimeSessionRequest): Promise<void> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime stop session is not implemented",
    );
  }

  async prepareWorkspace(
    request: PrepareRuntimeWorkspaceRequest,
  ): Promise<PreparedAgentWorkspace | undefined> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime prepare workspace is not implemented",
    );
  }

  async removeWorkspace(request: RemoveRuntimeWorkspaceRequest): Promise<void> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime remove workspace is not implemented",
    );
  }

  async startApp(request: StartHostedAppRequest): Promise<HostedApp> {
    throw new GestaltError(
      GestaltErrorCode.Unimplemented,
      "runtime start app is not implemented",
    );
  }
}

/**
 * createRuntimeProviderService adapts a RuntimeProvider handler to the connect-es
 * ServiceImpl surface for Runtime, converting wire types via the generated
 * codec and mapping handler errors to ConnectError.
 */
export function createRuntimeProviderService(
  handler: RuntimeProvider,
): Partial<ServiceImpl<typeof wire.Runtime>> {
  return {
    async getSupport(_request) {
      try {
        const response = await handler.getSupport();
        return toWireRuntimeSupport(response);
      } catch (error) {
        throw statusError("runtime get support", error);
      }
    },
    async startSession(request) {
      try {
        const response = await handler.startSession(
          fromWireStartRuntimeSessionRequest(request),
        );
        return toWireRuntimeSession(response);
      } catch (error) {
        throw statusError("runtime start session", error);
      }
    },
    async getSession(request) {
      try {
        const response = await handler.getSession(
          fromWireGetRuntimeSessionRequest(request),
        );
        return toWireRuntimeSession(response);
      } catch (error) {
        throw statusError("runtime get session", error);
      }
    },
    async listSessions(request) {
      try {
        const response = await handler.listSessions(
          fromWireListRuntimeSessionsRequest(request),
        );
        return toWireListRuntimeSessionsResponse(response);
      } catch (error) {
        throw statusError("runtime list sessions", error);
      }
    },
    async stopSession(request) {
      try {
        await handler.stopSession(fromWireStopRuntimeSessionRequest(request));
        return {};
      } catch (error) {
        throw statusError("runtime stop session", error);
      }
    },
    async prepareWorkspace(request) {
      try {
        const response = await handler.prepareWorkspace(
          fromWirePrepareRuntimeWorkspaceRequest(request),
        );
        return toWirePrepareRuntimeWorkspaceResponse({ workspace: response });
      } catch (error) {
        throw statusError("runtime prepare workspace", error);
      }
    },
    async removeWorkspace(request) {
      try {
        await handler.removeWorkspace(
          fromWireRemoveRuntimeWorkspaceRequest(request),
        );
        return {};
      } catch (error) {
        throw statusError("runtime remove workspace", error);
      }
    },
    async startApp(request) {
      try {
        const response = await handler.startApp(
          fromWireStartHostedAppRequest(request),
        );
        return toWireHostedApp(response);
      } catch (error) {
        throw statusError("runtime start app", error);
      }
    },
  };
}

// Code generated by sdkgen. DO NOT EDIT.

/**
 * Generated native types and client for workflow.proto.
 *
 * @module services/workflow
 */

import type { JsonObject, JsonValue } from "@bufbuild/protobuf";
import { createClient } from "@connectrpc/connect";
import type { Client, Transport } from "@connectrpc/connect";

import {
  createHostServiceGrpcTransport,
  hostServiceMetadataInterceptors,
  parseHostServiceTarget,
  requireHostServiceTarget,
} from "./host-service.ts";

import * as wire from "./internal/gen/v1/workflow_pb.ts";
import { type AgentOutput } from "./agent.ts";
import { type AgentToolRef, type RequestContext } from "./app.ts";
import {
  fromWireGetWorkflowProviderRunEventsResponse,
  fromWireGetWorkflowProviderRunOutputResponse,
  fromWireListWorkflowProviderDefinitionsResponse,
  fromWireListWorkflowProviderRunsResponse,
  fromWireSignalWorkflowRunResponse,
  fromWireWorkflowDefinition,
  fromWireWorkflowEvent,
  fromWireWorkflowRun,
  toWireApplyWorkflowProviderDefinitionRequest,
  toWireCancelWorkflowProviderRunRequest,
  toWireDeleteWorkflowProviderDefinitionRequest,
  toWireDeliverWorkflowProviderEventRequest,
  toWireGetWorkflowProviderDefinitionRequest,
  toWireGetWorkflowProviderRunEventsRequest,
  toWireGetWorkflowProviderRunOutputRequest,
  toWireGetWorkflowProviderRunRequest,
  toWireListWorkflowProviderDefinitionsRequest,
  toWireListWorkflowProviderRunsRequest,
  toWireSetWorkflowProviderActivationPausedRequest,
  toWireSetWorkflowProviderDefinitionPausedRequest,
  toWireSignalOrStartWorkflowProviderRunRequest,
  toWireSignalWorkflowProviderRunRequest,
  toWireStartWorkflowProviderRunRequest,
} from "./internal/codec/workflow.ts";
import { callOptions, callUnary } from "./internal/codec/support.ts";
import type { Init } from "./rpc_support.ts";

export const WorkflowRunStatus = {
  UNSPECIFIED: 0,
  PENDING: 1,
  RUNNING: 2,
  SUCCEEDED: 3,
  FAILED: 4,
  CANCELED: 5,
} as const;

export type WorkflowRunStatus = number;

export const WorkflowStepStatus = {
  UNSPECIFIED: 0,
  PENDING: 1,
  RUNNING: 2,
  SKIPPED: 3,
  SUCCEEDED: 4,
  FAILED: 5,
  UNKNOWN: 6,
} as const;

export type WorkflowStepStatus = number;

export interface ApplyWorkflowProviderDefinitionRequest {
  providerName: string;
  spec?: WorkflowDefinitionSpec;
  idempotencyKey: string;
  context?: RequestContext;
}

export interface BoundWorkflowTarget {
  steps: WorkflowStep[];
}

export interface CancelWorkflowProviderRunRequest {
  runId: string;
  reason: string;
  context?: RequestContext;
}

export interface DeleteWorkflowProviderDefinitionRequest {
  definitionId: string;
  context?: RequestContext;
}

export interface DeliverWorkflowProviderEventRequest {
  providerName: string;
  event?: WorkflowEvent;
  context?: RequestContext;
}

export interface GetWorkflowProviderDefinitionRequest {
  definitionId: string;
  context?: RequestContext;
}

export interface GetWorkflowProviderRunEventsRequest {
  runId: string;
  context?: RequestContext;
}

export interface GetWorkflowProviderRunEventsResponse {
  events: WorkflowRunEvent[];
}

export interface GetWorkflowProviderRunOutputRequest {
  runId: string;
  context?: RequestContext;
}

export interface GetWorkflowProviderRunOutputResponse {
  output?: JsonValue;
}

export interface GetWorkflowProviderRunRequest {
  runId: string;
  context?: RequestContext;
}

export interface ListWorkflowProviderDefinitionsRequest {
  context?: RequestContext;
}

export interface ListWorkflowProviderDefinitionsResponse {
  definitions: WorkflowDefinition[];
}

export interface ListWorkflowProviderRunsRequest {
  pageSize: number;
  pageToken: string;
  status: WorkflowRunStatus;
  targetApp: string;
  context?: RequestContext;
}

export interface ListWorkflowProviderRunsResponse {
  runs: WorkflowRun[];
  nextPageToken: string;
}

export interface SetWorkflowProviderActivationPausedRequest {
  definitionId: string;
  activationId: string;
  paused: boolean;
  context?: RequestContext;
}

export interface SetWorkflowProviderDefinitionPausedRequest {
  definitionId: string;
  paused: boolean;
  context?: RequestContext;
}

export interface SignalOrStartWorkflowProviderRunRequest {
  workflowKey: string;
  idempotencyKey: string;
  signal?: WorkflowSignal;
  providerName: string;
  definitionId: string;
  input?: JsonObject;
  expectedDefinitionGeneration: bigint;
  context?: RequestContext;
}

export interface SignalWorkflowProviderRunRequest {
  runId: string;
  signal?: WorkflowSignal;
  context?: RequestContext;
}

export interface SignalWorkflowRunResponse {
  run?: WorkflowRun;
  signal?: WorkflowSignal;
  startedRun: boolean;
  workflowKey: string;
}

export interface StartWorkflowProviderRunRequest {
  idempotencyKey: string;
  workflowKey: string;
  providerName: string;
  definitionId: string;
  input?: JsonObject;
  expectedDefinitionGeneration: bigint;
  context?: RequestContext;
}

export type WorkflowActivationTrigger =
  | { case: "schedule"; value: WorkflowScheduleActivation }
  | { case: "event"; value: WorkflowEventActivation }
  | { case: undefined; value?: undefined };

export function workflowActivationTriggerSchedule(
  value: WorkflowScheduleActivation,
): WorkflowActivationTrigger {
  return { case: "schedule", value };
}

export function workflowActivationTriggerEvent(
  value: WorkflowEventActivation,
): WorkflowActivationTrigger {
  return { case: "event", value };
}

export interface WorkflowActivation {
  id: string;
  input?: WorkflowValue;
  paused: boolean;
  trigger: WorkflowActivationTrigger;
}

export interface WorkflowAgentMessage {
  role: string;
  text?: WorkflowText;
  metadata?: JsonObject;
}

export interface WorkflowArray {
  values: WorkflowValue[];
}

export interface WorkflowDefinition {
  id: string;
  generation: bigint;
  target?: BoundWorkflowTarget;
  activations: WorkflowActivation[];
  paused: boolean;
  createdAt?: Date;
  updatedAt?: Date;
  providerName: string;
  runAs: string;
}

export interface WorkflowDefinitionSpec {
  id: string;
  target?: BoundWorkflowTarget;
  activations: WorkflowActivation[];
  paused: boolean;
  runAs: string;
}

export interface WorkflowEvent {
  id: string;
  source: string;
  specVersion: string;
  type: string;
  subject: string;
  time?: Date;
  datacontenttype: string;
  data?: JsonObject;
  extensions: { [key: string]: JsonValue };
}

export interface WorkflowEventActivation {
  match?: WorkflowEventMatch;
}

export interface WorkflowEventMatch {
  type: string;
  source: string;
  subject: string;
}

export interface WorkflowEventTriggerInvocation {
  activationId: string;
  event?: WorkflowEvent;
}

export interface WorkflowManualTrigger {}

export interface WorkflowObject {
  fields: { [key: string]: WorkflowValue };
}

export interface WorkflowPathSource {
  path: string;
}

export interface WorkflowRun {
  id: string;
  status: WorkflowRunStatus;
  target?: BoundWorkflowTarget;
  trigger?: WorkflowRunTrigger;
  createdAt?: Date;
  startedAt?: Date;
  completedAt?: Date;
  statusMessage: string;
  output?: JsonValue;
  workflowKey: string;
  providerName: string;
  definitionId: string;
  runAs: string;
  input?: JsonObject;
  definitionGeneration: bigint;
  currentStepId: string;
  steps: WorkflowStepExecution[];
}

export interface WorkflowRunEvent {
  id: string;
  runId: string;
  stepId: string;
  type: string;
  data?: JsonObject;
  createdAt?: Date;
}

export type WorkflowRunTriggerKind =
  | { case: "manual"; value: WorkflowManualTrigger }
  | { case: "schedule"; value: WorkflowScheduleTrigger }
  | { case: "event"; value: WorkflowEventTriggerInvocation }
  | { case: undefined; value?: undefined };

export function workflowRunTriggerKindManual(
  value: WorkflowManualTrigger,
): WorkflowRunTriggerKind {
  return { case: "manual", value };
}

export function workflowRunTriggerKindSchedule(
  value: WorkflowScheduleTrigger,
): WorkflowRunTriggerKind {
  return { case: "schedule", value };
}

export function workflowRunTriggerKindEvent(
  value: WorkflowEventTriggerInvocation,
): WorkflowRunTriggerKind {
  return { case: "event", value };
}

export interface WorkflowRunTrigger {
  kind: WorkflowRunTriggerKind;
}

export interface WorkflowScheduleActivation {
  cron: string;
  timezone: string;
}

export interface WorkflowScheduleTrigger {
  activationId: string;
  scheduledFor?: Date;
}

export interface WorkflowSignal {
  id: string;
  name: string;
  payload?: JsonObject;
  metadata?: JsonObject;
  createdAt?: Date;
  idempotencyKey: string;
  sequence: bigint;
}

export type WorkflowStepAction =
  | { case: "app"; value: WorkflowStepAppCall }
  | { case: "agent"; value: WorkflowStepAgentTurn }
  | { case: undefined; value?: undefined };

export function workflowStepActionApp(
  value: WorkflowStepAppCall,
): WorkflowStepAction {
  return { case: "app", value };
}

export function workflowStepActionAgent(
  value: WorkflowStepAgentTurn,
): WorkflowStepAction {
  return { case: "agent", value };
}

export interface WorkflowStep {
  id: string;
  inputs: { [key: string]: WorkflowValue };
  when?: WorkflowStepWhen;
  timeoutSeconds: number;
  metadata?: JsonObject;
  action: WorkflowStepAction;
}

export interface WorkflowStepAgentTurn {
  provider: string;
  model: string;
  sessionKey: string;
  prompt?: WorkflowText;
  messages: WorkflowAgentMessage[];
  tools: AgentToolRef[];
  output?: AgentOutput;
  modelOptions?: JsonObject;
}

export interface WorkflowStepAppCall {
  name: string;
  operation: string;
  input?: WorkflowValue;
  connection: string;
  instance: string;
  credentialMode: string;
}

export interface WorkflowStepAttempt {
  id: string;
  status: WorkflowStepStatus;
  idempotencyKey: string;
  input?: JsonValue;
  output?: JsonValue;
  statusMessage: string;
  startedAt?: Date;
  completedAt?: Date;
}

export interface WorkflowStepExecution {
  stepId: string;
  status: WorkflowStepStatus;
  attempts: WorkflowStepAttempt[];
  input?: JsonValue;
  output?: JsonValue;
  statusMessage: string;
  skipReason: string;
  startedAt?: Date;
  completedAt?: Date;
}

export interface WorkflowStepInputSource {
  stepId: string;
  path: string;
}

export interface WorkflowStepOutputSource {
  stepId: string;
  path: string;
}

export interface WorkflowStepWhen {
  value?: WorkflowValue;
  equals?: JsonValue;
}

export interface WorkflowText {
  template: string;
}

export type WorkflowValueKind =
  | { case: "literal"; value: JsonValue }
  | { case: "object"; value: WorkflowObject }
  | { case: "array"; value: WorkflowArray }
  | { case: "template"; value: WorkflowText }
  | { case: "input"; value: WorkflowPathSource }
  | { case: "signal"; value: WorkflowPathSource }
  | { case: "stepOutput"; value: WorkflowStepOutputSource }
  | { case: "stepInput"; value: WorkflowStepInputSource }
  | { case: undefined; value?: undefined };

export function workflowValueKindLiteral(value: JsonValue): WorkflowValueKind {
  return { case: "literal", value };
}

export function workflowValueKindObject(
  value: WorkflowObject,
): WorkflowValueKind {
  return { case: "object", value };
}

export function workflowValueKindArray(
  value: WorkflowArray,
): WorkflowValueKind {
  return { case: "array", value };
}

export function workflowValueKindTemplate(
  value: WorkflowText,
): WorkflowValueKind {
  return { case: "template", value };
}

export function workflowValueKindInput(
  value: WorkflowPathSource,
): WorkflowValueKind {
  return { case: "input", value };
}

export function workflowValueKindSignal(
  value: WorkflowPathSource,
): WorkflowValueKind {
  return { case: "signal", value };
}

export function workflowValueKindStepOutput(
  value: WorkflowStepOutputSource,
): WorkflowValueKind {
  return { case: "stepOutput", value };
}

export function workflowValueKindStepInput(
  value: WorkflowStepInputSource,
): WorkflowValueKind {
  return { case: "stepInput", value };
}

export interface WorkflowValue {
  kind: WorkflowValueKind;
}

export class Workflow {
  private readonly client: Client<typeof wire.Workflow>;
  private readonly timeoutMs: number | undefined;
  private readonly context: RequestContext | undefined;

  constructor(
    transport: Transport,
    options?: {
      context?: RequestContext | undefined;
      timeoutMs?: number | undefined;
    },
  ) {
    this.client = createClient(wire.Workflow, transport);
    this.context = options?.context;
    this.timeoutMs = options?.timeoutMs;
  }

  static connect(options?: {
    name?: string | undefined;
    context?: RequestContext | undefined;
    timeoutMs?: number | undefined;
  }): Workflow {
    const { target, token } = requireHostServiceTarget("workflow");
    const transport = createHostServiceGrpcTransport(
      parseHostServiceTarget("workflow", target),
      hostServiceMetadataInterceptors(token, options?.name?.trim() ?? ""),
    );
    return new Workflow(transport, options);
  }

  async applyDefinition(
    providerName: string,
    idempotencyKey: string,
    spec?: Init<WorkflowDefinitionSpec>,
  ): Promise<WorkflowDefinition> {
    const request = {
      providerName,
      idempotencyKey,
      ...(spec !== undefined ? { spec } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ApplyWorkflowProviderDefinitionRequest>;
    const response = await callUnary(() =>
      this.client.applyDefinition(
        toWireApplyWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async applyDefinitionRaw(
    request: Init<ApplyWorkflowProviderDefinitionRequest>,
  ): Promise<WorkflowDefinition> {
    const response = await callUnary(() =>
      this.client.applyDefinition(
        toWireApplyWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async getDefinition(definitionId: string): Promise<WorkflowDefinition> {
    const request = {
      definitionId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetWorkflowProviderDefinitionRequest>;
    const response = await callUnary(() =>
      this.client.getDefinition(
        toWireGetWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async getDefinitionRaw(
    request: Init<GetWorkflowProviderDefinitionRequest>,
  ): Promise<WorkflowDefinition> {
    const response = await callUnary(() =>
      this.client.getDefinition(
        toWireGetWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async listDefinitions(
    request: Init<ListWorkflowProviderDefinitionsRequest>,
  ): Promise<WorkflowDefinition[]> {
    const response = fromWireListWorkflowProviderDefinitionsResponse(
      await callUnary(() =>
        this.client.listDefinitions(
          toWireListWorkflowProviderDefinitionsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.definitions;
  }

  async listDefinitionsRaw(
    request: Init<ListWorkflowProviderDefinitionsRequest>,
  ): Promise<ListWorkflowProviderDefinitionsResponse> {
    const response = await callUnary(() =>
      this.client.listDefinitions(
        toWireListWorkflowProviderDefinitionsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListWorkflowProviderDefinitionsResponse(response);
  }

  async setDefinitionPaused(
    definitionId: string,
    paused: boolean,
  ): Promise<WorkflowDefinition> {
    const request = {
      definitionId,
      paused,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<SetWorkflowProviderDefinitionPausedRequest>;
    const response = await callUnary(() =>
      this.client.setDefinitionPaused(
        toWireSetWorkflowProviderDefinitionPausedRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async setDefinitionPausedRaw(
    request: Init<SetWorkflowProviderDefinitionPausedRequest>,
  ): Promise<WorkflowDefinition> {
    const response = await callUnary(() =>
      this.client.setDefinitionPaused(
        toWireSetWorkflowProviderDefinitionPausedRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async setActivationPaused(
    definitionId: string,
    activationId: string,
    paused: boolean,
  ): Promise<WorkflowDefinition> {
    const request = {
      definitionId,
      activationId,
      paused,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<SetWorkflowProviderActivationPausedRequest>;
    const response = await callUnary(() =>
      this.client.setActivationPaused(
        toWireSetWorkflowProviderActivationPausedRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async setActivationPausedRaw(
    request: Init<SetWorkflowProviderActivationPausedRequest>,
  ): Promise<WorkflowDefinition> {
    const response = await callUnary(() =>
      this.client.setActivationPaused(
        toWireSetWorkflowProviderActivationPausedRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowDefinition(response);
  }

  async deleteDefinition(definitionId: string): Promise<void> {
    const request = {
      definitionId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<DeleteWorkflowProviderDefinitionRequest>;
    await callUnary(() =>
      this.client.deleteDefinition(
        toWireDeleteWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
  }

  async deleteDefinitionRaw(
    request: Init<DeleteWorkflowProviderDefinitionRequest>,
  ): Promise<void> {
    await callUnary(() =>
      this.client.deleteDefinition(
        toWireDeleteWorkflowProviderDefinitionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
  }

  async startRun(
    idempotencyKey: string,
    workflowKey: string,
    providerName: string,
    definitionId: string,
    expectedDefinitionGeneration: bigint,
    input?: JsonObject,
  ): Promise<WorkflowRun> {
    const request = {
      idempotencyKey,
      workflowKey,
      providerName,
      definitionId,
      expectedDefinitionGeneration,
      ...(input !== undefined ? { input } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<StartWorkflowProviderRunRequest>;
    const response = await callUnary(() =>
      this.client.startRun(
        toWireStartWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async startRunRaw(
    request: Init<StartWorkflowProviderRunRequest>,
  ): Promise<WorkflowRun> {
    const response = await callUnary(() =>
      this.client.startRun(
        toWireStartWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async listRuns(
    pageSize: number,
    pageToken: string,
    status: WorkflowRunStatus,
    targetApp: string,
  ): Promise<ListWorkflowProviderRunsResponse> {
    const request = {
      pageSize,
      pageToken,
      status,
      targetApp,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ListWorkflowProviderRunsRequest>;
    const response = await callUnary(() =>
      this.client.listRuns(
        toWireListWorkflowProviderRunsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListWorkflowProviderRunsResponse(response);
  }

  async listRunsRaw(
    request: Init<ListWorkflowProviderRunsRequest>,
  ): Promise<ListWorkflowProviderRunsResponse> {
    const response = await callUnary(() =>
      this.client.listRuns(
        toWireListWorkflowProviderRunsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListWorkflowProviderRunsResponse(response);
  }

  async getRun(runId: string): Promise<WorkflowRun> {
    const request = {
      runId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetWorkflowProviderRunRequest>;
    const response = await callUnary(() =>
      this.client.getRun(
        toWireGetWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async getRunRaw(
    request: Init<GetWorkflowProviderRunRequest>,
  ): Promise<WorkflowRun> {
    const response = await callUnary(() =>
      this.client.getRun(
        toWireGetWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async getRunEvents(runId: string): Promise<WorkflowRunEvent[]> {
    const request = {
      runId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetWorkflowProviderRunEventsRequest>;
    const response = fromWireGetWorkflowProviderRunEventsResponse(
      await callUnary(() =>
        this.client.getRunEvents(
          toWireGetWorkflowProviderRunEventsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.events;
  }

  async getRunEventsRaw(
    request: Init<GetWorkflowProviderRunEventsRequest>,
  ): Promise<GetWorkflowProviderRunEventsResponse> {
    const response = await callUnary(() =>
      this.client.getRunEvents(
        toWireGetWorkflowProviderRunEventsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireGetWorkflowProviderRunEventsResponse(response);
  }

  async getRunOutput(runId: string): Promise<JsonValue | undefined> {
    const request = {
      runId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetWorkflowProviderRunOutputRequest>;
    const response = fromWireGetWorkflowProviderRunOutputResponse(
      await callUnary(() =>
        this.client.getRunOutput(
          toWireGetWorkflowProviderRunOutputRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.output;
  }

  async getRunOutputRaw(
    request: Init<GetWorkflowProviderRunOutputRequest>,
  ): Promise<GetWorkflowProviderRunOutputResponse> {
    const response = await callUnary(() =>
      this.client.getRunOutput(
        toWireGetWorkflowProviderRunOutputRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireGetWorkflowProviderRunOutputResponse(response);
  }

  async cancelRun(runId: string, reason: string): Promise<WorkflowRun> {
    const request = {
      runId,
      reason,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<CancelWorkflowProviderRunRequest>;
    const response = await callUnary(() =>
      this.client.cancelRun(
        toWireCancelWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async cancelRunRaw(
    request: Init<CancelWorkflowProviderRunRequest>,
  ): Promise<WorkflowRun> {
    const response = await callUnary(() =>
      this.client.cancelRun(
        toWireCancelWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowRun(response);
  }

  async signalRun(
    runId: string,
    signal?: Init<WorkflowSignal>,
  ): Promise<SignalWorkflowRunResponse> {
    const request = {
      runId,
      ...(signal !== undefined ? { signal } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<SignalWorkflowProviderRunRequest>;
    const response = await callUnary(() =>
      this.client.signalRun(
        toWireSignalWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireSignalWorkflowRunResponse(response);
  }

  async signalRunRaw(
    request: Init<SignalWorkflowProviderRunRequest>,
  ): Promise<SignalWorkflowRunResponse> {
    const response = await callUnary(() =>
      this.client.signalRun(
        toWireSignalWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireSignalWorkflowRunResponse(response);
  }

  async signalOrStartRun(
    workflowKey: string,
    idempotencyKey: string,
    providerName: string,
    definitionId: string,
    expectedDefinitionGeneration: bigint,
    signal?: Init<WorkflowSignal>,
    input?: JsonObject,
  ): Promise<SignalWorkflowRunResponse> {
    const request = {
      workflowKey,
      idempotencyKey,
      providerName,
      definitionId,
      expectedDefinitionGeneration,
      ...(signal !== undefined ? { signal } : {}),
      ...(input !== undefined ? { input } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<SignalOrStartWorkflowProviderRunRequest>;
    const response = await callUnary(() =>
      this.client.signalOrStartRun(
        toWireSignalOrStartWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireSignalWorkflowRunResponse(response);
  }

  async signalOrStartRunRaw(
    request: Init<SignalOrStartWorkflowProviderRunRequest>,
  ): Promise<SignalWorkflowRunResponse> {
    const response = await callUnary(() =>
      this.client.signalOrStartRun(
        toWireSignalOrStartWorkflowProviderRunRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireSignalWorkflowRunResponse(response);
  }

  async deliverEvent(
    providerName: string,
    event?: Init<WorkflowEvent>,
  ): Promise<WorkflowEvent> {
    const request = {
      providerName,
      ...(event !== undefined ? { event } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<DeliverWorkflowProviderEventRequest>;
    const response = await callUnary(() =>
      this.client.deliverEvent(
        toWireDeliverWorkflowProviderEventRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowEvent(response);
  }

  async deliverEventRaw(
    request: Init<DeliverWorkflowProviderEventRequest>,
  ): Promise<WorkflowEvent> {
    const response = await callUnary(() =>
      this.client.deliverEvent(
        toWireDeliverWorkflowProviderEventRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireWorkflowEvent(response);
  }
}

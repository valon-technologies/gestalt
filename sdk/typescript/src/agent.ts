// Code generated by sdkgen. DO NOT EDIT.

import type { JsonObject, JsonValue } from "@bufbuild/protobuf";
import { createClient } from "@connectrpc/connect";
import type { Client, Transport } from "@connectrpc/connect";

import {
  createHostServiceGrpcTransport,
  hostServiceMetadataInterceptors,
  parseHostServiceTarget,
  requireHostServiceTarget,
} from "./host-service.ts";

import * as wire from "./internal/gen/v1/agent_pb.ts";
import {
  type AgentToolRef,
  type OperationAnnotations,
  type RequestContext,
  type SubjectContext,
} from "./app.ts";
import {
  fromWireAgentInteraction,
  fromWireAgentProviderCapabilities,
  fromWireAgentSession,
  fromWireAgentTurn,
  fromWireListAgentProviderInteractionsResponse,
  fromWireListAgentProviderSessionsResponse,
  fromWireListAgentProviderTurnEventsResponse,
  fromWireListAgentProviderTurnsResponse,
  toWireCancelAgentProviderTurnRequest,
  toWireCreateAgentProviderSessionRequest,
  toWireCreateAgentProviderTurnRequest,
  toWireGetAgentProviderCapabilitiesRequest,
  toWireGetAgentProviderInteractionRequest,
  toWireGetAgentProviderSessionRequest,
  toWireGetAgentProviderTurnRequest,
  toWireListAgentProviderInteractionsRequest,
  toWireListAgentProviderSessionsRequest,
  toWireListAgentProviderTurnEventsRequest,
  toWireListAgentProviderTurnsRequest,
  toWireResolveAgentProviderInteractionRequest,
  toWireUpdateAgentProviderSessionRequest,
} from "./internal/codec/agent.ts";
import { callOptions, callUnary } from "./internal/codec/support.ts";
import type { Init } from "./rpc_support.ts";

export const AgentExecutionStatus = {
  UNSPECIFIED: 0,
  PENDING: 1,
  RUNNING: 2,
  SUCCEEDED: 3,
  FAILED: 4,
  CANCELED: 5,
  WAITING_FOR_INPUT: 6,
} as const;

export type AgentExecutionStatus = number;

export const AgentInteractionState = {
  UNSPECIFIED: 0,
  PENDING: 1,
  RESOLVED: 2,
  CANCELED: 3,
} as const;

export type AgentInteractionState = number;

export const AgentInteractionType = {
  UNSPECIFIED: 0,
  APPROVAL: 1,
  CLARIFICATION: 2,
  INPUT: 3,
} as const;

export type AgentInteractionType = number;

export const AgentMessagePartType = {
  UNSPECIFIED: 0,
  TEXT: 1,
  JSON: 2,
  TOOL_CALL: 3,
  TOOL_RESULT: 4,
  IMAGE_REF: 5,
} as const;

export type AgentMessagePartType = number;

export const AgentSessionState = {
  UNSPECIFIED: 0,
  ACTIVE: 1,
  ARCHIVED: 2,
} as const;

export type AgentSessionState = number;

export const AgentToolSourceMode = {
  UNSPECIFIED: 0,
  CATALOG: 2,
  NONE: 3,
} as const;

export type AgentToolSourceMode = number;

export interface AgentCatalogToolConfig {
  refs: AgentToolRef[];
  tools: ListedAgentTool[];
}

export interface AgentInteraction {
  id: string;
  type: AgentInteractionType;
  state: AgentInteractionState;
  title: string;
  prompt: string;
  request?: JsonObject;
  resolution?: JsonObject;
  createdAt?: Date;
  resolvedAt?: Date;
  turnId: string;
  sessionId: string;
}

export interface AgentMessage {
  role: string;
  text: string;
  parts: AgentMessagePart[];
  metadata?: JsonObject;
}

export interface AgentMessagePart {
  type: AgentMessagePartType;
  text: string;
  json?: JsonObject;
  toolCall?: AgentMessagePartToolCall;
  toolResult?: AgentMessagePartToolResult;
  imageRef?: AgentMessagePartImageRef;
}

export interface AgentMessagePartImageRef {
  uri: string;
  mimeType: string;
}

export interface AgentMessagePartToolCall {
  id: string;
  toolId: string;
  arguments?: JsonObject;
}

export interface AgentMessagePartToolResult {
  toolCallId: string;
  status: number;
  content: string;
  output?: JsonObject;
}

export interface AgentNoTools {}

export type AgentOutputKind =
  | { case: "text"; value: AgentTextOutput }
  | { case: "structured"; value: AgentStructuredOutput }
  | { case: undefined; value?: undefined };

export function agentOutputKindText(value: AgentTextOutput): AgentOutputKind {
  return { case: "text", value };
}

export function agentOutputKindStructured(
  value: AgentStructuredOutput,
): AgentOutputKind {
  return { case: "structured", value };
}

export interface AgentOutput {
  kind: AgentOutputKind;
}

export interface AgentProviderCapabilities {
  streamingText: boolean;
  toolCalls: boolean;
  parallelToolCalls: boolean;
  interactions: boolean;
  resumableTurns: boolean;
  reasoningSummaries: boolean;
  /**
   * Provider list APIs can apply non-zero limits and summary projections without
   * hydrating every source record. Providers that set this must order sessions
   * and turns by the relevant newest-first recency fields before applying limit.
   */
  boundedListHydration: boolean;
  supportedToolSources: AgentToolSourceMode[];
  supportsSessionStart: boolean;
  supportsPreparedWorkspace: boolean;
}

export interface AgentSession {
  id: string;
  providerName: string;
  model: string;
  clientRef: string;
  state: AgentSessionState;
  metadata?: JsonObject;
  createdBySubjectId: string;
  createdAt?: Date;
  updatedAt?: Date;
  lastTurnAt?: Date;
}

export interface AgentSessionStartConfig {
  hooks: AgentSessionStartHook[];
}

export interface AgentSessionStartHook {
  id: string;
  type: string;
  command: string[];
  cwd: string;
  timeout: string;
  env: { [key: string]: string };
  output?: AgentSessionStartHookOutput;
}

export interface AgentSessionStartHookOutput {
  additionalContext: boolean;
  metadata: boolean;
}

export interface AgentStructuredOutput {
  schema?: JsonObject;
}

export interface AgentTextOutput {}

export type AgentToolConfigSource =
  | { case: "none"; value: AgentNoTools }
  | { case: "catalog"; value: AgentCatalogToolConfig }
  | { case: undefined; value?: undefined };

export function agentToolConfigSourceNone(
  value: AgentNoTools,
): AgentToolConfigSource {
  return { case: "none", value };
}

export function agentToolConfigSourceCatalog(
  value: AgentCatalogToolConfig,
): AgentToolConfigSource {
  return { case: "catalog", value };
}

export interface AgentToolConfig {
  source: AgentToolConfigSource;
}

export type AgentTurnOutput =
  | { case: "text"; value: AgentTurnTextOutput }
  | { case: "structured"; value: AgentTurnStructuredOutput }
  | { case: undefined; value?: undefined };

export function agentTurnOutputText(
  value: AgentTurnTextOutput,
): AgentTurnOutput {
  return { case: "text", value };
}

export function agentTurnOutputStructured(
  value: AgentTurnStructuredOutput,
): AgentTurnOutput {
  return { case: "structured", value };
}

export interface AgentTurn {
  id: string;
  sessionId: string;
  providerName: string;
  model: string;
  status: AgentExecutionStatus;
  messages: AgentMessage[];
  statusMessage: string;
  createdBySubjectId: string;
  createdAt?: Date;
  startedAt?: Date;
  completedAt?: Date;
  executionRef: string;
  output: AgentTurnOutput;
}

export interface AgentTurnDisplay {
  kind: string;
  phase: string;
  text: string;
  label: string;
  ref: string;
  parentRef: string;
  input?: JsonValue;
  output?: JsonValue;
  error?: JsonValue;
  action: string;
  format: string;
  language: string;
}

export interface AgentTurnEvent {
  id: string;
  turnId: string;
  seq: bigint;
  type: string;
  source: string;
  visibility: string;
  data?: JsonObject;
  createdAt?: Date;
  display?: AgentTurnDisplay;
}

export interface AgentTurnStructuredOutput {
  text: string;
  value?: JsonObject;
}

export interface AgentTurnTextOutput {
  text: string;
}

export interface AgentWorkspace {
  checkouts: AgentWorkspaceGitCheckout[];
  cwd: string;
}

export interface AgentWorkspaceGitCheckout {
  url: string;
  ref: string;
  path: string;
}

export interface CancelAgentProviderTurnRequest {
  turnId: string;
  reason: string;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

export interface CreateAgentProviderSessionRequest {
  /**
   * The provider mints the session id returned on AgentSession. Creation is
   * idempotent on idempotency_key scoped per subject (created_by_subject_id):
   * a replayed key returns the existing session, an empty key always creates.
   * Idempotency is scoped to the provider's session store.
   */
  idempotencyKey: string;
  model: string;
  clientRef: string;
  metadata?: JsonObject;
  createdBySubjectId: string;
  subject?: SubjectContext;
  sessionStart?: AgentSessionStartConfig;
  preparedWorkspace?: PreparedAgentWorkspace;
  providerName: string;
  workspace?: AgentWorkspace;
  context?: RequestContext;
  tools?: AgentToolConfig;
}

export interface CreateAgentProviderTurnRequest {
  turnId: string;
  sessionId: string;
  idempotencyKey: string;
  model: string;
  messages: AgentMessage[];
  metadata?: JsonObject;
  createdBySubjectId: string;
  executionRef: string;
  subject?: SubjectContext;
  modelOptions?: JsonObject;
  /**
   * Optional provider-owned turn execution budget, in seconds.
   * If unset or zero, the provider chooses its own execution timeout. This does
   * not control the CreateTurn RPC deadline.
   */
  timeoutSeconds: number;
  output?: AgentOutput;
  context?: RequestContext;
  providerName: string;
}

export interface GetAgentProviderCapabilitiesRequest {}

export interface GetAgentProviderInteractionRequest {
  interactionId: string;
  subject?: SubjectContext;
  context?: RequestContext;
}

export interface GetAgentProviderSessionRequest {
  sessionId: string;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

export interface GetAgentProviderTurnRequest {
  turnId: string;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

export interface ListAgentProviderInteractionsRequest {
  turnId: string;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

export interface ListAgentProviderInteractionsResponse {
  interactions: AgentInteraction[];
}

export interface ListAgentProviderSessionsRequest {
  subject?: SubjectContext;
  sessionIds: string[];
  state: AgentSessionState;
  /**
   * When non-zero and bounded_list_hydration is supported, cap results after
   * ordering sessions newest-first by last_turn_at, updated_at, then created_at.
   */
  limit: number;
  /**
   * When true and bounded_list_hydration is supported, omit heavy fields such as
   * metadata unless exact session_ids require direct lookup.
   */
  summaryOnly: boolean;
  providerName: string;
  context?: RequestContext;
}

export interface ListAgentProviderSessionsResponse {
  sessions: AgentSession[];
}

export interface ListAgentProviderTurnEventsRequest {
  turnId: string;
  afterSeq: bigint;
  limit: number;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

export interface ListAgentProviderTurnEventsResponse {
  events: AgentTurnEvent[];
}

export interface ListAgentProviderTurnsRequest {
  sessionId: string;
  subject?: SubjectContext;
  turnIds: string[];
  status: AgentExecutionStatus;
  /**
   * When non-zero and bounded_list_hydration is supported, cap results after
   * ordering turns newest-first by created_at.
   */
  limit: number;
  /**
   * When true and bounded_list_hydration is supported, omit heavy fields such as
   * messages, output text, and structured output unless exact turn_ids require
   * direct lookup.
   */
  summaryOnly: boolean;
  context?: RequestContext;
  providerName: string;
}

export interface ListAgentProviderTurnsResponse {
  turns: AgentTurn[];
}

export interface ListedAgentTool {
  id: string;
  mcpName: string;
  title: string;
  description: string;
  inputSchema: string;
  outputSchema: string;
  annotations?: OperationAnnotations;
  ref?: AgentToolRef;
  tags: string[];
  searchText: string;
}

export interface PreparedAgentWorkspace {
  root: string;
  cwd: string;
}

export interface ResolveAgentProviderInteractionRequest {
  interactionId: string;
  resolution?: JsonObject;
  subject?: SubjectContext;
  turnId: string;
  context?: RequestContext;
  providerName: string;
}

export interface UpdateAgentProviderSessionRequest {
  sessionId: string;
  clientRef: string;
  state: AgentSessionState;
  metadata?: JsonObject;
  subject?: SubjectContext;
  context?: RequestContext;
  providerName: string;
}

/**
 * Agent is the authoritative agent data boundary. Read RPCs for
 * sessions, turns, turn events, and interactions should use provider-owned
 * control-plane state and should not require a live execution sandbox,
 * pod-level transport, or cached tunnel.
 */
export class Agent {
  private readonly client: Client<typeof wire.Agent>;
  private readonly timeoutMs: number | undefined;
  private readonly context: RequestContext | undefined;

  constructor(
    transport: Transport,
    options?: { context?: RequestContext; timeoutMs?: number },
  ) {
    this.client = createClient(wire.Agent, transport);
    this.context = options?.context;
    this.timeoutMs = options?.timeoutMs;
  }

  static connect(
    name?: string,
    options?: { context?: RequestContext; timeoutMs?: number },
  ): Agent {
    const { target, token } = requireHostServiceTarget("agent");
    const transport = createHostServiceGrpcTransport(
      parseHostServiceTarget("agent", target),
      hostServiceMetadataInterceptors(token, name?.trim() ?? ""),
    );
    return new Agent(transport, options);
  }

  async createSession(
    providerName: string,
    idempotencyKey: string,
    model: string,
    options?: {
      clientRef?: string;
      metadata?: JsonObject;
      workspace?: Init<AgentWorkspace>;
      tools?: Init<AgentToolConfig>;
    },
  ): Promise<AgentSession> {
    const request = {
      providerName,
      idempotencyKey,
      model,
      clientRef: options?.clientRef ?? "",
      createdBySubjectId: "",
      ...(options?.metadata !== undefined
        ? { metadata: options.metadata }
        : {}),
      ...(options?.workspace !== undefined
        ? { workspace: options.workspace }
        : {}),
      ...(options?.tools !== undefined ? { tools: options.tools } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<CreateAgentProviderSessionRequest>;
    const response = await callUnary(() =>
      this.client.createSession(
        toWireCreateAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async createSessionRaw(
    request: Init<CreateAgentProviderSessionRequest>,
  ): Promise<AgentSession> {
    const response = await callUnary(() =>
      this.client.createSession(
        toWireCreateAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async getSession(
    sessionId: string,
    options?: { providerName?: string },
  ): Promise<AgentSession> {
    const request = {
      sessionId,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetAgentProviderSessionRequest>;
    const response = await callUnary(() =>
      this.client.getSession(
        toWireGetAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async getSessionRaw(
    request: Init<GetAgentProviderSessionRequest>,
  ): Promise<AgentSession> {
    const response = await callUnary(() =>
      this.client.getSession(
        toWireGetAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async listSessions(options?: {
    sessionIds?: string[];
    state?: AgentSessionState;
    limit?: number;
    summaryOnly?: boolean;
    providerName?: string;
  }): Promise<AgentSession[]> {
    const request = {
      sessionIds: options?.sessionIds ?? [],
      state: options?.state ?? 0,
      limit: options?.limit ?? 0,
      summaryOnly: options?.summaryOnly ?? false,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ListAgentProviderSessionsRequest>;
    const response = fromWireListAgentProviderSessionsResponse(
      await callUnary(() =>
        this.client.listSessions(
          toWireListAgentProviderSessionsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.sessions;
  }

  async listSessionsRaw(
    request: Init<ListAgentProviderSessionsRequest>,
  ): Promise<ListAgentProviderSessionsResponse> {
    const response = await callUnary(() =>
      this.client.listSessions(
        toWireListAgentProviderSessionsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListAgentProviderSessionsResponse(response);
  }

  async updateSession(
    sessionId: string,
    options?: {
      clientRef?: string;
      state?: AgentSessionState;
      providerName?: string;
      metadata?: JsonObject;
    },
  ): Promise<AgentSession> {
    const request = {
      sessionId,
      clientRef: options?.clientRef ?? "",
      state: options?.state ?? 0,
      providerName: options?.providerName ?? "",
      ...(options?.metadata !== undefined
        ? { metadata: options.metadata }
        : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<UpdateAgentProviderSessionRequest>;
    const response = await callUnary(() =>
      this.client.updateSession(
        toWireUpdateAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async updateSessionRaw(
    request: Init<UpdateAgentProviderSessionRequest>,
  ): Promise<AgentSession> {
    const response = await callUnary(() =>
      this.client.updateSession(
        toWireUpdateAgentProviderSessionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentSession(response);
  }

  async createTurn(
    sessionId: string,
    idempotencyKey: string,
    model: string,
    messages: Init<AgentMessage>[],
    options?: {
      executionRef?: string;
      timeoutSeconds?: number;
      providerName?: string;
      metadata?: JsonObject;
      modelOptions?: JsonObject;
      output?: Init<AgentOutput>;
    },
  ): Promise<AgentTurn> {
    const request = {
      sessionId,
      idempotencyKey,
      model,
      messages,
      executionRef: options?.executionRef ?? "",
      timeoutSeconds: options?.timeoutSeconds ?? 0,
      providerName: options?.providerName ?? "",
      turnId: "",
      createdBySubjectId: "",
      ...(options?.metadata !== undefined
        ? { metadata: options.metadata }
        : {}),
      ...(options?.modelOptions !== undefined
        ? { modelOptions: options.modelOptions }
        : {}),
      ...(options?.output !== undefined ? { output: options.output } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<CreateAgentProviderTurnRequest>;
    const response = await callUnary(() =>
      this.client.createTurn(
        toWireCreateAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async createTurnRaw(
    request: Init<CreateAgentProviderTurnRequest>,
  ): Promise<AgentTurn> {
    const response = await callUnary(() =>
      this.client.createTurn(
        toWireCreateAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async getTurn(
    turnId: string,
    options?: { providerName?: string },
  ): Promise<AgentTurn> {
    const request = {
      turnId,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetAgentProviderTurnRequest>;
    const response = await callUnary(() =>
      this.client.getTurn(
        toWireGetAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async getTurnRaw(
    request: Init<GetAgentProviderTurnRequest>,
  ): Promise<AgentTurn> {
    const response = await callUnary(() =>
      this.client.getTurn(
        toWireGetAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async listTurns(
    sessionId: string,
    options?: {
      turnIds?: string[];
      status?: AgentExecutionStatus;
      limit?: number;
      summaryOnly?: boolean;
      providerName?: string;
    },
  ): Promise<AgentTurn[]> {
    const request = {
      sessionId,
      turnIds: options?.turnIds ?? [],
      status: options?.status ?? 0,
      limit: options?.limit ?? 0,
      summaryOnly: options?.summaryOnly ?? false,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ListAgentProviderTurnsRequest>;
    const response = fromWireListAgentProviderTurnsResponse(
      await callUnary(() =>
        this.client.listTurns(
          toWireListAgentProviderTurnsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.turns;
  }

  async listTurnsRaw(
    request: Init<ListAgentProviderTurnsRequest>,
  ): Promise<ListAgentProviderTurnsResponse> {
    const response = await callUnary(() =>
      this.client.listTurns(
        toWireListAgentProviderTurnsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListAgentProviderTurnsResponse(response);
  }

  async cancelTurn(
    turnId: string,
    options?: { reason?: string; providerName?: string },
  ): Promise<AgentTurn> {
    const request = {
      turnId,
      reason: options?.reason ?? "",
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<CancelAgentProviderTurnRequest>;
    const response = await callUnary(() =>
      this.client.cancelTurn(
        toWireCancelAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async cancelTurnRaw(
    request: Init<CancelAgentProviderTurnRequest>,
  ): Promise<AgentTurn> {
    const response = await callUnary(() =>
      this.client.cancelTurn(
        toWireCancelAgentProviderTurnRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentTurn(response);
  }

  async listTurnEvents(
    turnId: string,
    options?: { afterSeq?: bigint; limit?: number; providerName?: string },
  ): Promise<AgentTurnEvent[]> {
    const request = {
      turnId,
      afterSeq: options?.afterSeq ?? 0n,
      limit: options?.limit ?? 0,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ListAgentProviderTurnEventsRequest>;
    const response = fromWireListAgentProviderTurnEventsResponse(
      await callUnary(() =>
        this.client.listTurnEvents(
          toWireListAgentProviderTurnEventsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.events;
  }

  async listTurnEventsRaw(
    request: Init<ListAgentProviderTurnEventsRequest>,
  ): Promise<ListAgentProviderTurnEventsResponse> {
    const response = await callUnary(() =>
      this.client.listTurnEvents(
        toWireListAgentProviderTurnEventsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListAgentProviderTurnEventsResponse(response);
  }

  async getInteraction(interactionId: string): Promise<AgentInteraction> {
    const request = {
      interactionId,
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<GetAgentProviderInteractionRequest>;
    const response = await callUnary(() =>
      this.client.getInteraction(
        toWireGetAgentProviderInteractionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentInteraction(response);
  }

  async getInteractionRaw(
    request: Init<GetAgentProviderInteractionRequest>,
  ): Promise<AgentInteraction> {
    const response = await callUnary(() =>
      this.client.getInteraction(
        toWireGetAgentProviderInteractionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentInteraction(response);
  }

  async listInteractions(
    turnId: string,
    options?: { providerName?: string },
  ): Promise<AgentInteraction[]> {
    const request = {
      turnId,
      providerName: options?.providerName ?? "",
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ListAgentProviderInteractionsRequest>;
    const response = fromWireListAgentProviderInteractionsResponse(
      await callUnary(() =>
        this.client.listInteractions(
          toWireListAgentProviderInteractionsRequest({
            ...request,
            ...(request.context === undefined && this.context !== undefined
              ? { context: this.context }
              : {}),
          }),
          callOptions(this.timeoutMs),
        ),
      ),
    );
    return response.interactions;
  }

  async listInteractionsRaw(
    request: Init<ListAgentProviderInteractionsRequest>,
  ): Promise<ListAgentProviderInteractionsResponse> {
    const response = await callUnary(() =>
      this.client.listInteractions(
        toWireListAgentProviderInteractionsRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireListAgentProviderInteractionsResponse(response);
  }

  async resolveInteraction(
    interactionId: string,
    resolution?: JsonObject,
    options?: { turnId?: string; providerName?: string },
  ): Promise<AgentInteraction> {
    const request = {
      interactionId,
      turnId: options?.turnId ?? "",
      providerName: options?.providerName ?? "",
      ...(resolution !== undefined ? { resolution } : {}),
      ...(this.context !== undefined ? { context: this.context } : {}),
    } satisfies Init<ResolveAgentProviderInteractionRequest>;
    const response = await callUnary(() =>
      this.client.resolveInteraction(
        toWireResolveAgentProviderInteractionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentInteraction(response);
  }

  async resolveInteractionRaw(
    request: Init<ResolveAgentProviderInteractionRequest>,
  ): Promise<AgentInteraction> {
    const response = await callUnary(() =>
      this.client.resolveInteraction(
        toWireResolveAgentProviderInteractionRequest({
          ...request,
          ...(request.context === undefined && this.context !== undefined
            ? { context: this.context }
            : {}),
        }),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentInteraction(response);
  }

  async getCapabilities(
    request: Init<GetAgentProviderCapabilitiesRequest>,
  ): Promise<AgentProviderCapabilities> {
    const response = await callUnary(() =>
      this.client.getCapabilities(
        toWireGetAgentProviderCapabilitiesRequest(request),
        callOptions(this.timeoutMs),
      ),
    );
    return fromWireAgentProviderCapabilities(response);
  }
}

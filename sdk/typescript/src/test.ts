// Code generated by sdkgen. DO NOT EDIT.

import { createClient } from "@connectrpc/connect";
import type { Client, Transport } from "@connectrpc/connect";

import * as wire from "./internal/gen/v1/test_pb.ts";
import { fromWireHelloWorldResponse, toWireHelloWorldRequest } from "./internal/codec/test.ts";
import { callUnary } from "./internal/codec/support.ts";

/**
 * HelloWorldRequest carries no input fields. It exists to exercise the
 * provider-kind-specific request/response path.
 */
export interface HelloWorldRequest {
}

/**
 * HelloWorldResponse returns the fixed test-provider message.
 */
export interface HelloWorldResponse {
  message: string;
}

/**
 * Test models a minimal provider-kind-specific protocol used to verify new
 * provider kind registration and lifecycle wiring.
 */
export class Test {
  private readonly client: Client<typeof wire.Test>;

  constructor(transport: Transport) {
    this.client = createClient(wire.Test, transport);
  }

  async helloWorld(request: HelloWorldRequest): Promise<string> {
    const response = fromWireHelloWorldResponse(await callUnary(() => this.client.helloWorld(toWireHelloWorldRequest(request))));
    return response.message;
  }

  async helloWorldRaw(request: HelloWorldRequest): Promise<HelloWorldResponse> {
    const response = await callUnary(() => this.client.helloWorld(toWireHelloWorldRequest(request)));
    return fromWireHelloWorldResponse(response);
  }

}

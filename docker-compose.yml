services:
  gestaltd:
    build:
      context: .
      dockerfile: Dockerfile.gestaltd
    ports:
      - "8080:8080"
    volumes:
      - ./deploy/docker/config.yaml:/etc/gestalt/config.yaml:ro
      - gestalt-data:/data
    env_file:
      - .env

volumes:
  gestalt-data:

apiVersion: v1
kind: Service
metadata:
  name: {{ include "toolshed.fullname" . }}
  labels:
    {{- include "toolshed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "toolshed.selectorLabels" . | nindent 4 }}

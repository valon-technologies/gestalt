apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "toolshed.fullname" . }}-config
  labels:
    {{- include "toolshed.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}

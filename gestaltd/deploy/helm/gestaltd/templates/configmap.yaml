apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gestaltd.fullname" . }}-config
  labels:
    {{- include "gestaltd.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}

name: Build Gestalt CLI

on:
  push:
    branches:
      - main
    paths:
      - 'client/**'
      - '.github/workflows/build-gestalt-cli.yml'
  pull_request:
    paths:
      - 'client/**'

  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Rust Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: client

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            client/target/
          key: ${{ runner.os }}-cargo-gestalt-cli-${{ hashFiles('client/**/Cargo.toml') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Enforce Functional-Only Test Policy
        run: bash ../scripts/check-functional-tests.sh

      - name: Run tests
        run: cargo test --workspace

  build:
    needs: lint
    uses: ./.github/workflows/rust-build-matrix.yml
    with:
      working-directory: client
      binary-name: gestalt
      artifact-prefix: gestalt
      run-tests: true
      package: false

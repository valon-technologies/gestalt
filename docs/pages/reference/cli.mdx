# CLI

{/* Auto-generated by `cargo xtask` from clap derive structs. Do not edit by hand. */}

## `gestalt` Client CLI

This document contains the help content for the `gestalt` command-line program.

**Command Overview:**

* [`gestalt`↴](#gestalt)
* [`gestalt auth`↴](#gestalt-auth)
* [`gestalt auth login`↴](#gestalt-auth-login)
* [`gestalt auth logout`↴](#gestalt-auth-logout)
* [`gestalt auth status`↴](#gestalt-auth-status)
* [`gestalt init`↴](#gestalt-init)
* [`gestalt config`↴](#gestalt-config)
* [`gestalt config get`↴](#gestalt-config-get)
* [`gestalt config set`↴](#gestalt-config-set)
* [`gestalt config unset`↴](#gestalt-config-unset)
* [`gestalt config list`↴](#gestalt-config-list)
* [`gestalt integrations`↴](#gestalt-integrations)
* [`gestalt integrations list`↴](#gestalt-integrations-list)
* [`gestalt integrations connect`↴](#gestalt-integrations-connect)
* [`gestalt invoke`↴](#gestalt-invoke)
* [`gestalt describe`↴](#gestalt-describe)
* [`gestalt tokens`↴](#gestalt-tokens)
* [`gestalt tokens create`↴](#gestalt-tokens-create)
* [`gestalt tokens list`↴](#gestalt-tokens-list)
* [`gestalt tokens revoke`↴](#gestalt-tokens-revoke)

### `gestalt`

CLI for Gestalt API - authentication, integrations, and operations

**Usage:** `gestalt [OPTIONS] <COMMAND>`

###### **Subcommands:**

* `auth` — Manage authentication (login, logout)
* `init` — Interactive setup wizard
* `config` — Manage persistent configuration
* `integrations` — Manage third-party integrations
* `invoke` — Execute an integration operation
* `describe` — Describe an integration operation
* `tokens` — Manage API tokens

###### **Options:**

* `--format <FORMAT>` — Output format

  Default value: `table`

  Possible values: `json`, `table`

* `--url <URL>` — API server URL (overrides config and env)

### `gestalt auth`

Manage authentication (login, logout)

**Usage:** `gestalt auth <COMMAND>`

###### **Subcommands:**

* `login` — Log in via browser OAuth flow
* `logout` — Log out and clear stored credentials
* `status` — Show authentication status

### `gestalt auth login`

Log in via browser OAuth flow

**Usage:** `gestalt auth login`

### `gestalt auth logout`

Log out and clear stored credentials

**Usage:** `gestalt auth logout`

### `gestalt auth status`

Show authentication status

**Usage:** `gestalt auth status`

### `gestalt init`

Interactive setup wizard

**Usage:** `gestalt init`

### `gestalt config`

Manage persistent configuration

**Usage:** `gestalt config <COMMAND>`

###### **Subcommands:**

* `get` — Get a config value
* `set` — Set a config value
* `unset` — Remove a config value
* `list` — List all config values

### `gestalt config get`

Get a config value

**Usage:** `gestalt config get <KEY>`

###### **Arguments:**

* `<KEY>` — Config key

### `gestalt config set`

Set a config value

**Usage:** `gestalt config set <KEY> <VALUE>`

###### **Arguments:**

* `<KEY>` — Config key
* `<VALUE>` — Config value

### `gestalt config unset`

Remove a config value

**Usage:** `gestalt config unset <KEY>`

###### **Arguments:**

* `<KEY>` — Config key

### `gestalt config list`

List all config values

**Usage:** `gestalt config list`

### `gestalt integrations`

Manage third-party integrations

**Usage:** `gestalt integrations <COMMAND>`

###### **Subcommands:**

* `list` — List available integrations
* `connect` — Connect an integration via OAuth

### `gestalt integrations list`

List available integrations

**Usage:** `gestalt integrations list`

### `gestalt integrations connect`

Connect an integration via OAuth

**Usage:** `gestalt integrations connect <NAME>`

###### **Arguments:**

* `<NAME>` — Integration name (e.g., github, slack)

### `gestalt invoke`

Execute an integration operation

**Usage:** `gestalt invoke [OPTIONS] <INTEGRATION> [OPERATION]`

###### **Arguments:**

* `<INTEGRATION>` — Integration name (e.g., github, slack)
* `<OPERATION>` — Operation name (e.g., search_code, list_channels). Omit to list available operations

###### **Options:**

* `-p`, `--param <PARAMS>` — Parameters as key=value or key:=json pairs
* `--select <SELECT>` — Select a sub-path from the response (e.g., "data.items")
* `--input-file <INPUT_FILE>` — Load parameters from a JSON file (use "-" for stdin)

### `gestalt describe`

Describe an integration operation

**Usage:** `gestalt describe <INTEGRATION> <OPERATION>`

###### **Arguments:**

* `<INTEGRATION>` — Integration name
* `<OPERATION>` — Operation name

### `gestalt tokens`

Manage API tokens

**Usage:** `gestalt tokens <COMMAND>`

###### **Subcommands:**

* `create` — Create a new API token
* `list` — List all API tokens
* `revoke` — Revoke an API token

### `gestalt tokens create`

Create a new API token

**Usage:** `gestalt tokens create [OPTIONS]`

###### **Options:**

* `--name <NAME>` — Display name for the token

### `gestalt tokens list`

List all API tokens

**Usage:** `gestalt tokens list`

### `gestalt tokens revoke`

Revoke an API token

**Usage:** `gestalt tokens revoke <ID>`

###### **Arguments:**

* `<ID>` — Token ID to revoke

## `gestaltd` Server CLI

The `gestaltd` server binary.

## Usage

```sh
gestaltd [--config PATH]
gestaltd init [--config PATH]
gestaltd serve [--config PATH] [--locked]
gestaltd validate [--config PATH] [--init]
gestaltd plugin package [flags]
gestaltd version
```

## Commands

| Command | Purpose |
| --- | --- |
| `gestaltd` | Default start. Auto-generates a local config if none exists. Auto-prepares missing state. |
| `gestaltd init` | Resolve remote providers and packaged plugins and write lock state. |
| `gestaltd serve` | Start the server from an explicit config path. |
| `gestaltd serve --locked` | Start the server. Fail if prepared state is missing or stale. |
| `gestaltd validate` | Load and validate config without starting the server. |
| `gestaltd plugin package` | Package a plugin directory or binary. |
| `gestaltd version` | Print the binary version and exit. |

## Default Command Behavior

The plain `gestaltd` command has one extra convenience:

- If you do not pass `--config` and no config file exists in the search path, it generates `~/.gestalt/config.yaml`

That generated config is local-only:

- `none` auth
- SQLite datastore
- `env` secrets
- A generated `server.encryption_key`

## Config Path Resolution

When a command needs a config path:

1. `--config` flag
2. `GESTALT_CONFIG` environment variable
3. `./config.yaml`
4. `~/.gestalt/config.yaml`
5. `/etc/gestalt/config.yaml`

## `init`

```sh
gestaltd init --config ./config.yaml
```

Resolves remote providers and packaged plugins and writes lock state next to the config:

- `gestalt.lock.json`
- `.gestalt/providers/*.json` (prepared provider artifacts)
- `.gestalt/plugins/...` (installed plugin packages)

Run `init` whenever your config contains remote REST upstreams, remote GraphQL upstreams, `plugin.package`, or `plugin.source`.

## `serve --locked`

Production startup:

```sh
gestaltd init --config ./config.yaml
gestaltd serve --locked --config ./config.yaml
```

If prepared state is missing or stale, startup fails instead of mutating local state.

## `validate`

Non-mutating config validation. Expects lock state to already exist when the config depends on prepared artifacts.

```sh
gestaltd init --config ./config.yaml
gestaltd validate --config ./config.yaml
```

### `validate --init`

Run initialization before validating:

```sh
gestaltd validate --init --config ./config.yaml
```

## `plugin package`

Two packaging flows:

### From a manifest directory

Package an existing plugin directory that already contains a `plugin.json` manifest:

```sh
gestaltd plugin package --input ./build/my-plugin --output ./dist/my-plugin
```

### From a pre-built binary

Scaffold a manifest and package a binary in one step:

```sh
gestaltd plugin package \
  --binary ./my-plugin \
  --source github.com/myorg/my-repo/my-plugin \
  --output ./dist/my-plugin
```

Output format is determined by the `--output` path: paths ending in `.tar.gz` produce an archive for remote distribution, all other paths produce a directory.

| Flag | Default | Purpose |
| --- | --- | --- |
| `--binary` | — | Path to pre-built binary. |
| `--source` | — | Plugin source identifier (`github.com/owner/repo/plugin`). Required with `--binary`. |
| `--output` | — | Output path (directory, or `.tar.gz` for archive). |
| `--kind` | `provider` | Plugin kind: `provider` or `runtime`. |
| `--os` | Current platform | Target OS for the artifact. |
| `--arch` | Current platform | Target architecture. |
| `--version` | `0.0.0-alpha.1` | Manifest version. Must be valid SemVer. |

Cross-compilation example:

```sh
GOOS=linux GOARCH=amd64 go build -o ./my-plugin-linux .
gestaltd plugin package \
  --binary ./my-plugin-linux \
  --source github.com/myorg/my-repo/my-plugin \
  --os linux --arch amd64 \
  --output ./dist/my-plugin
```
